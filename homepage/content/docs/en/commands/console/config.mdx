---
title: sma console config
description: Manage project ship.json and alias (model pool is separate)
---

# `sma console config`

Manage project `ship.json` and alias.

> Naming note: configuration command is `sma console config`, not `sma config`.

## Usage

```bash
sma console config <subcommand>
```

## Subcommands

- `get [keyPath]`
- `set <keyPath> <value>`
- `unset <keyPath>`
- `alias`

> Model pool management has moved to [`sma console model`](/en/docs/commands/console/model).

## `extensions.*` path behavior

When `keyPath` starts with `extensions.`, the command automatically reads/writes console-global `~/.ship/ship.db` (`console_config`).

```bash
sma console config get extensions.voice
sma console config set extensions.voice.enabled true
```

## Examples

```bash
sma console config get model.primary
sma console config set model.primary fast
sma console config unset services.chat.channels.telegram
sma console config alias --dry-run
```

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `package/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ sma console config get model.primary
{
  "success": true,
  "projectRoot": "<projectRoot>",
  "shipJsonPath": "<projectRoot>/ship.json",
  "keyPath": "model.primary",
  "value": "<modelId>"
}
```

```text
$ sma console config set model.primary gpt-5.4
{
  "success": true,
  "projectRoot": "<projectRoot>",
  "shipJsonPath": "<projectRoot>/ship.json",
  "keyPath": "model.primary",
  "value": "gpt-5.4",
  "existed": true,
  "previous": "<oldModelId>"
}
```
