# cli/town

`cli/town` 是 Downcity 仓库内部的本机 Agent 宿主命令构建单元。

```bash
npm install -g downcity
town -v
```

安装 `downcity` 后会同时得到：

- `town`：本机 Agent 宿主与运行入口。
- `city`：City 管理入口。

`cli/town` 自身标记为 private，不作为独立 npm 包发布。
