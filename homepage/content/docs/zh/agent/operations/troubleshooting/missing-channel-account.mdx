---
title: channel account 缺失
description: 项目里绑定了不存在的 channelAccountId 时如何处理
---

# channel account 缺失

如果项目里配置了不存在的 `channelAccountId`，聊天渠道不会正常工作。

## 排查点

- `downcity.json` 里绑定的 ID 是否拼写正确
- 全局层是否真的存在这个账号
- 修改后是否已经重启 agent

如果一个团队要长期维护多个 Agent，建议先把 account id 命名规则统一好，这会大幅降低后续排障成本。
