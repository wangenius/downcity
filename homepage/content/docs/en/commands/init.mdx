---
title: studio init
description: Initialize global console config (stored in ~/.downcity/downcity.db)
---

# `studio init`

Initialize global console infrastructure.

## Usage

```bash
studio init
```

## Behavior

- Writes/updates `~/.downcity/downcity.db` (global console infrastructure)
- Generates `~/.downcity/schema/downcity.schema.json` (editor schema)

## Notes

- This is a console-level command and does not depend on an agent project path.
- For project-level configuration, use [`studio config`](/en/docs/commands/config).
- `studio init` does not create or update models.
- Configure models later with `studio model create`.

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `packages/downcity/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ studio init
downcity v1.0.0
Console base initialized
Console initialized
```

```text
$ studio init
downcity v1.0.0
Console base initialized
Console initialized
```
