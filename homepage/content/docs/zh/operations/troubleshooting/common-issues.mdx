---
title: 常见问题
description: 安装、启动与配置中的常见错误
---

# 常见问题

## 命令找不到（`bay: command not found` / `bay: command not found`）

```bash
npm config get prefix
echo $PATH
export PATH=$(npm config get prefix)/bin:$PATH
rehash
bay --version
```

或直接用：

```bash
bay --version
```

说明：

- `bay` 和 `city` 来自同一个 `downcity` 包，但负责不同命令面。
- 新版本安装后不应该再需要手动 `chmod +x`。
- 如果 `sudo bay ...` 失败，但普通 `bay ...` 正常，通常是 `sudo` 的 PATH 问题，不是安装失败。

## 端口冲突（5314 被占用）

```bash
lsof -nP -iTCP:5314 -sTCP:LISTEN
bay agent doctor . --fix
bay agent restart .
```

## 启动后无法调用 `bay task/chat/...`

先确认 daemon 运行中：

```bash
bay agent start
bay plugin list
```

## LLM 配置错误

检查：

```bash
bay model list
bay config get model.primary
```

并确认：

1. `model.primary` 指向 `~/.downcity/downcity.db` 中存在的 model id
2. console 全局 SQLite 的 providers/models 记录完整可用
3. 如果是 Kimi（Moonshot），`provider.type` 请使用 `moonshot-cn` 或 `moonshot-ai`
4. `moonshot-cn` 默认走 `https://api.moonshot.cn/v1`
5. `moonshot-ai` 默认走 `https://api.moonshot.ai/v1`
6. Kimi 模型名请按对应站点填写，例如 `kimi-k2-0905-preview` 或 `kimi-k2.5`
