# Skill Plugin

skill 是你拥有的技能。用户的提出的需求和任务，你都需要先考虑利用相关的skill来高质量的完成。


## 使用

使用技能时需要首先通过 `city skill lookup <name>` 命令载入对应的技能内容。

你也可以使用 `city skill --help` 命令对skill进行其他操作。比如 `list`, `find`, `install`等等。
