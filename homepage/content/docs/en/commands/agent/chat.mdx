---
title: city agent chat
description: Open a persistent terminal chat with a selected agent
---

# `city agent chat`

Open a persistent terminal chat with a target agent.

This command reuses the same main session as Console UI: `consoleui-chat-main`.

## Usage

```bash
city agent chat [--to <agentName>]
```

## Key options

- `-t, --to <name>`: target agent name; if omitted, the CLI opens an interactive picker from running agents
- `--host <host>` / `--port <port>`: force remote HTTP transport
- `--token <token>`: explicit Bearer token for remote HTTP mode

## Behavior

- Requires an interactive terminal.
- Requires the target agent to be running.
- Keeps sending every line into the same shared Console main session.
- Type `/exit` or `/quit` to leave the chat loop.

## Related commands

- [`city agent list`](/en/docs/commands/agent/list)
- [`city agent quest`](/en/docs/commands/agent/quest)
- [`city agent start`](/en/docs/commands/agent/start)

## Example

```text
$ city agent chat --to lucas_whitman
downcity v1.0.439

INFO Agent chat · lucas_whitman

lucas_whitman> summarize today's work

Today you completed the plugin interactive CLI redesign and validated the updated command tree.
```
