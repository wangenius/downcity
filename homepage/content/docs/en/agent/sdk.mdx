---
title: Agent SDK
description: Embed the Downcity Agent runtime in a Node application
---

# Agent SDK

Use `@downcity/agent` to create an Agent, manage Sessions, and execute model calls in the same process. See the [Agent SDK docs](/en/agent-sdk-docs) for the complete API.

```ts
import { Agent } from "@downcity/agent";

const agent = new Agent({
  id: "repo-helper",
  path: process.cwd(),
  model,
  tools: { search: search_tool },
  plugins: [plugin],
  env: { API_KEY: process.env.API_KEY ?? "" },
});

const session = await agent.sessions.create();
const turn = await session.prompt({ query: "Inspect this project" });
const result = await turn.finished;

await agent.dispose();
```

Construction starts plugin lifecycle and background work. Use `await agent.ready()` when an explicit readiness barrier is needed, and `await agent.dispose()` to release resources. HTTP and RPC are supplied by `AgentHTTP` and `AgentRPC` from `@downcity/server`.

## Streaming messages

`subscribe()` returns the unified live Session Mutation stream:

```ts
const unsubscribe = session.subscribe((mutation) => {
  if (mutation.variant === "delta" && mutation.type === "text") {
    process.stdout.write(mutation.delta);
  }
});
```

Tool lifecycle updates use `variant: "part"` and `type: "tool"`. An unrestricted request that is waiting for a decision includes the complete approval details in `mutation.part.approval`; submit the decision through the Session command API rather than replying through the event callback:

Part order within an Assistant step always follows the model stream. If Tool execution becomes ready before its Tool Part reaches the stream, execution waits at that boundary instead of overtaking preceding text. Waits are isolated by `tool_call_id`, so multiple Tools can still execute concurrently. Clients should render Mutations and history snapshots in their recorded order without reordering them.

```ts
const unsubscribe = session.subscribe((mutation) => {
  if (
    mutation.variant === "part" &&
    mutation.type === "tool" &&
    mutation.part.state === "approval-required" &&
    mutation.part.approval
  ) {
    const { approval_id, command, cwd, reason } = mutation.part.approval;
    show_approval({ approval_id, command, cwd, reason });
  }
});

async function decide_approval(approval_id: string, decision: "approved" | "denied") {
  await session.resolve_approval({ approval_id, decision });
}
```

Use `session.messages()` for a history snapshot. After a disconnect, reload the snapshot and establish a new subscription.

Continue with the [full Agent SDK docs](/en/agent-sdk-docs).
