---
title: city agents
description: Replaced by city agent list --running
---

# `city agents`

This command is no longer part of the public command tree.

Use [`city agent list`](/en/docs/commands/agent/list) instead.

```bash
city agent list --running
```
