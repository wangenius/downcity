---
title: CLI FAQ
description: Common questions about town agent commands
---

# CLI FAQ

## What is the difference between `town agent start` and `town agent start --foreground`?

- the default mode is a background daemon
- `--foreground` runs in the current terminal

## Why does `town agent chat` say it cannot connect?

Usually because the target agent is not running.

Check:

```bash
town agent status
```

If the state is not `running`, restore that first before troubleshooting chat context or prompts.
