---
title: Agent 生命周期
description: 从初始化到启动、聊天、停机，理解一个 Agent 的完整生命周期
---

# Agent 生命周期

一个项目 agent 的典型生命周期可以拆成 5 步。

## 1. 初始化项目

初始化会生成最小可运行骨架，包括：

- `PROFILE.md`
- `SOUL.md`
- `downcity.json`
- `.downcity/`

这一步的目标是把一个普通目录变成“可以被 Downcity 识别和启动的 agent 项目”，而不是一次性把所有外部依赖都配置完。

## 2. 配置执行目标

当前 agent 项目只有一种执行绑定：

```json
{
  "execution": {
    "type": "api",
    "modelId": "your-model-id"
  }
}
```

也就是说，项目要绑定到已连接 Federation AIService 中的某个模型 ID。

在当前代码里，这个执行绑定不是可选装饰，而是启动链路的硬前提之一。

## 3. 启动运行时

启动时，agent 会：

- 校验项目是否初始化完成
- 读取 `downcity.json`
- 载入 `PROFILE.md` / `SOUL.md`
- 解析模型绑定
- 启动 HTTP 服务
- 启动项目内配置的 plugin

如果使用后台模式，Downcity 还会为它维护 daemon 状态，让生命周期操作可以继续检查、停止、重启或修复这个进程。

## 4. 执行和交互

运行中的 agent 可以通过多种入口执行：

- Downcity CLI
- 已接入的聊天渠道
- 项目级 HTTP 控制接口
- SDK `Agent` / `RemoteAgent`

无论从哪个入口进入，都会回到同一套会话和执行内核。

这意味着 CLI、聊天渠道、项目控制接口和 SDK 虽然看起来是不同入口，但对 agent 来说，它们更多是“不同的调用入口”，而不是“不同的产品”。

## 5. 停止、重启和恢复

停止、重启和恢复，本质上都是对同一个项目运行时做生命周期管理。如果进程异常退出但状态文件仍在，恢复操作会让 daemon 状态和真实进程重新一致。

## 日常迭代里最常见的生命周期循环

在真实使用中，Agent 的生命周期通常不是单次直线流程，而是一个循环：

1. 改 `PROFILE.md`
2. 改 `downcity.json`
3. 重启项目运行时
4. 和 agent 交互
5. 看输出，再继续微调

如果你在打磨一个长期运行的项目 agent，这个循环会非常常见。

## 具体工作流在哪里

这一页讲的是生命周期模型。具体命令工作流放在：

- [Agent CLI 产品文档](/zh/docs/reference/cli)
- [Downcity City 产品文档](/zh/docs/reference/cli)
- [Agent SDK 总览](/zh/agent-sdk-docs)
