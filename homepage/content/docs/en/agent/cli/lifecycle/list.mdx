---
title: bay agent list
description: List the Agent projects currently registered with city
---

# `bay agent list`

## Usage

```bash
bay agent list
bay agent list --running
bay agent list --json
```

## What it lists

This command shows agent projects registered in the city registry.

It does not mean "scan the current directory tree for repos."

That is why you may see:

- an agent listed in `list`
- but not currently in a `running` state

## `--running`

Only show currently running agents:

```bash
bay agent list --running
```

## `--json`

Use structured output when a script or toolchain will consume the result:

```bash
bay agent list --json
```

## When to use it

- when you are not sure which projects are already registered
- when you want to pick a target before `chat`
- when you want to inspect which agents are currently running

If you are unsure which agent to talk to, `list` followed by `chat --to ...` is usually the safest path.

Continue with:

- [bay agent start](/en/docs/agent/cli/lifecycle/start)
- [Agent Registry](/en/docs/agent/cli/maintenance/registry)
