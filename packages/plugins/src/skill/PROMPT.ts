/**
 * 自动生成文件，请勿手改。
 * 源文件：由同路径 `*.ts.txt` 生成。
 */

// Source: src/skill/PROMPT.ts.txt
const TEXT_MODULE_CONTENT = "# Skill Plugin\n\nskill 是你拥有的技能。用户的提出的需求和任务，你都需要先考虑利用相关的skill来高质量的完成。\n\n\n## 使用\n\n使用技能时需要首先通过 `bay skill lookup <name>` 命令载入对应的技能内容。\n\n你也可以使用 `bay skill --help` 命令对skill进行其他操作。比如 `list`, `find`, `install`等等。\n";

export default TEXT_MODULE_CONTENT;
