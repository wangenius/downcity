---
title: studio agent
description: Manage agents interactively, or use subcommands for scripts
---

# `studio agent`

Manage agent lifecycle and registry-aware operations.

> Recommended reading: the new complete Agent documentation now lives in a dedicated docs domain. Start with [Agent Overview](/en/docs/agent/overview/what-is-agent) and [Agent CLI Overview](/en/docs/agent/cli/overview).

Run `studio agent` without a subcommand in an interactive terminal to open the agent manager. Use subcommands when you want a scriptable command.

## Command hierarchy

Top-level command groups are: `studio console`, `studio agent`, `studio plugin`.

## Usage

```bash
studio agent
studio agent <subcommand>
```

## Version inspection

- `studio -v` shows the city CLI version only.
- `studio agent -v` shows both the current city version and the `@downcity/agent` runtime version actually bundled with that city installation.

## Interactive manager

The interactive manager lets you:

- list registered agents
- create an agent project
- start an agent
- pick an agent and see current status, name, model, and chat channel connections before choosing an action
- configure the agent id, model binding, or city-level chat channel account connections

It does not replace the subcommands below. It is a guided entry point for common local operations.

Chat channel credentials stay in city-level channel accounts managed from `studio chat`. The agent manager only connects an agent to those existing accounts through `plugins.chat.channels.<channel>.channelAccountId`. If an agent points at a missing account, the manager removes that dangling binding automatically.

## Subcommands

- `create`: create/initialize an agent project
- `list`: list registered agent projects
- `start`: start the agent
- `stop`: stop the agent daemon
- `chat`: chat with an agent in interactive or one-shot mode
- `status`: show agent status
- `doctor`: diagnose state files and optionally fix stale pid/meta
- `restart`: restart the agent

## Subcommand docs

- [`studio agent create`](/en/docs/commands/agent/create)
- [`studio agent list`](/en/docs/commands/agent/list)
- [`studio agent start`](/en/docs/commands/agent/start)
- [`studio agent stop`](/en/docs/commands/agent/stop)
- [`studio agent chat`](/en/docs/commands/agent/chat)
- [`studio agent status`](/en/docs/commands/agent/status)
- [`studio agent restart`](/en/docs/commands/agent/restart)
- [`studio agent doctor`](/en/docs/commands/agent/doctor)

## Environment variable injection

On every `studio` execution, CLI resolves current agent context and injects variables into the `studio` process and its child processes:

- `DC_AGENT_ID`: current agent runtime id
- `DC_AGENT_PATH`: absolute path of current agent

> Note: this is process-scoped injection and does not mutate your parent shell session.

## Related command

- Start console first: [`studio start`](/en/docs/commands/start)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `packages/downcity/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ studio agent
downcity v1.0.0
? 管理 Agent
  查看 agents
  创建 agent
  启动 agent
  管理 agent - status, model, channels are shown before actions
  退出
```
