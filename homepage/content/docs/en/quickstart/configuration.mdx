---
title: Configuration
description: downcity.json, PROFILE.md, and runtime configuration essentials
---

# Configuration

Downcity is configured with three core files:

- `PROFILE.md`: agent role and boundaries (required)
- `SOUL.md`: stable operating principles (optional)
- `downcity.json`: runtime configuration (required)

## Key `downcity.json` fields

### `start` (advanced only)

For `city agent start`, ports are auto-allocated by console.

So in normal usage you do not need `start.port` in project `downcity.json`.

Only configure `start` when manually running foreground runtime or debugging special host binding:

```json
{
  "start": {
    "host": "0.0.0.0"
  }
}
```

### `model`

Agent model binding (maps to console-global model pool):

```json
{
  "model": {
    "primary": "default"
  }
}
```

### `services.chat`

Chat channels and queue behavior:

```json
{
  "services": {
    "chat": {
      "method": "direct",
      "queue": {
        "maxConcurrency": 2,
        "mergeDebounceMs": 600,
        "mergeMaxWaitMs": 2000
      },
      "channels": {
        "telegram": {
          "enabled": true,
          "channelAccountId": "telegram-main"
        }
      }
    }
  }
}
```

`channelAccountId` binds to the global channel account registry in `~/.downcity/downcity.db` (`channel_accounts`).

`services.chat.queue` notes:
- `maxConcurrency`: max lane concurrency across different chat keys.
- `mergeDebounceMs`: short debounce window for batching consecutive inbound messages in one run.
- `mergeMaxWaitMs`: hard cap for the batching wait to avoid unbounded delay.

### `plugins.skill`

Skills discovery roots:

```json
{
  "plugins": {
    "skill": {
      "enabled": true,
      "paths": [".agents/skills"],
      "allowExternalPaths": false
    }
  }
}
```

### `plugins.*` / `assets.*`

Plugin config is maintained in project `downcity.json`.

Example for voice:

```bash
city voice on SenseVoiceSmall
city console config get plugins.voice
city console config get 'assets.voice.transcriber'
```

## Useful commands

```bash
city console config get
city console config get model.primary
city console model list
city console config unset services.chat.channels.telegram
```

## Next

- [CLI Reference](/en/docs/reference/cli)
- [`.downcity/` Directory](/en/docs/reference/dot-downcity)
- [Environment Variable Strategy (Console Shared vs Agent Private)](/en/docs/configuration/environment-variables)
