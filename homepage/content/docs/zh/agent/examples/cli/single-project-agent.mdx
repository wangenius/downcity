---
title: 例子：单项目 Agent
description: 把一个普通代码仓库初始化并启动成 Agent 的最短路径
---

# 例子：单项目 Agent

```bash
studio agent create .
studio agent start
studio agent chat
```

这就是最短路径。

建议第一次就只跑这条最短路径，不要一上来同时叠加多个渠道和复杂 plugin。

后续再逐步补：

- `PROFILE.md`
- `downcity.json`
- 聊天渠道绑定

这样每次只增加一个变量，问题会更容易定位。
