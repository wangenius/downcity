---
title: bay agent status
description: Show target agent status
---

# `bay agent status`

Show process status for a target agent.

> This page remains as a command entry point. For the fuller explanation, see [CLI / status](/en/docs/agent/cli/lifecycle/status) and [Agent Shows stale](/en/docs/agent/operations/troubleshooting/agent-shows-stale).

## Usage

```bash
bay agent status [path]
```

## Behavior

- `path` defaults to current directory `.`
- Target agent must be registered in console registry
- Output includes agent state, process info, and key paths
- When state is `stale`, output includes `stale_reason` for direct diagnosis

## Related commands

- [`bay agent start`](/en/docs/commands/agent/start)
- [`bay agent restart`](/en/docs/commands/agent/restart)
- [`bay agent list --running`](/en/docs/commands/agent/list)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `cli/bay/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ bay agent status .
bay v1.0.0
┌─────────────────────────────────────────────────────────┐
│ OK bay agent status                                    │
│   project    : <projectRoot>                           │
│   log        : <daemonLogPath>                         │
│   state      : running                                 │
│   pid        : <pid>                                   │
│   started_at : <isoTimestamp>                          │
└─────────────────────────────────────────────────────────┘
```

```text
$ bay agent status .
bay v1.0.0
┌─────────────────────────────────────────────────────────┐
│ WARN bay agent status                                  │
│   project      : <projectRoot>                         │
│   log          : <daemonLogPath>                       │
│   state        : stale                                 │
│   stale_pid    : <pid>                                 │
│   stale_reason : <reason>                              │
│   fix          : bay agent doctor <projectRoot> --fix  │
└─────────────────────────────────────────────────────────┘
```
