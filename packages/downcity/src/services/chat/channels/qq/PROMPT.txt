# QQ Adapter 使用说明

## 能力范围
- 负责 QQ 官方机器人网关（WebSocket）消息接入与发送。
- 入站消息会由 chat service 映射到内部 `sessionId`（随机分配并持久化）。

## 使用约束
- QQ 出站回复依赖入站消息上下文（如 `chatType` 与 `messageId`）；跨 session 发送时应使用已建立映射的 `sessionId`。
- 群聊消息默认全量接入，不需要 `@` 触发。
- 适配器会过滤机器人自回环消息并做入站去重，避免“自己回复自己”的循环。
