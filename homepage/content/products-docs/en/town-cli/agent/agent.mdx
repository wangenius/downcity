---
title: town agent
description: Manage agents interactively, or use subcommands for scripts
---

# `town agent`

Manage agent lifecycle and registry-aware operations.

> Recommended reading: the new complete Agent documentation now lives in a dedicated docs domain. Start with [Agent Overview](/en/docs/agent/overview/what-is-agent) and [Agent CLI Overview](/en/docs/agent/cli/overview).

Run `town agent` without a subcommand in an interactive terminal to open the agent manager. Use subcommands when you want a scriptable command.

## Command hierarchy

Core command groups are: `town start/stop/restart/status`, `town city`, `town agent`, `town plugin`, plus plugin shortcut roots.

## Usage

```bash
town agent
town agent <subcommand>
```

## Version inspection

- `town -v` shows the city CLI version only.
- `town agent -v` shows both the current town CLI version and the `@downcity/agent` runtime version actually bundled with that town installation.

## Interactive manager

The interactive manager lets you:

- list registered agents
- create an agent project
- start an agent
- pick an agent and see current status, name, City execution binding, and chat account connections before choosing an action
- configure the agent id or connect Town-level chat accounts

It does not replace the subcommands below. It is a guided entry point for common local operations.

Chat credentials stay in Town-level chat accounts managed from `town chat`. The agent manager only connects an agent to those existing accounts through `plugins.chat.channels.<platform>.channelAccountId`. If an agent points at a missing account, the manager removes that dangling binding automatically.

## Subcommands

- `create`: create/initialize an agent project
- `list`: list registered agent projects
- `start`: start the agent
- `stop`: stop the agent daemon
- `chat`: chat with an agent in interactive or one-shot mode
- `status`: show agent status
- `doctor`: diagnose state files and optionally fix stale pid/meta
- `restart`: restart the agent

## Subcommand docs

- [`town agent create`](/en/products-docs/town-cli/agent/create)
- [`town agent list`](/en/products-docs/town-cli/agent/list)
- [`town agent start`](/en/products-docs/town-cli/agent/start)
- [`town agent stop`](/en/products-docs/town-cli/agent/stop)
- [`town agent chat`](/en/products-docs/town-cli/agent/chat)
- [`town agent status`](/en/products-docs/town-cli/agent/status)
- [`town agent restart`](/en/products-docs/town-cli/agent/restart)
- [`town agent doctor`](/en/products-docs/town-cli/agent/doctor)

## Environment variable injection

On every `town` execution, CLI resolves current agent context and injects variables into the `town` process and its child processes:

- `DC_AGENT_ID`: current agent runtime id
- `DC_AGENT_PATH`: absolute path of current agent

> Note: this is process-scoped injection and does not mutate your parent shell session.

## Related command

- Start Town runtime first: [`town start`](/en/products-docs/town-cli/start)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `cli/town/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ town agent
town v1.0.0
? 管理 Agent
  查看 agents
  创建 agent
  启动 agent
  管理 agent - status, execution binding, chat accounts are shown before actions
  退出
```
