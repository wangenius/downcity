---
title: bay restart
description: Restart Bay runtime, restore managed agents, then restart Console
---

# `bay restart`

Restart the full Bay runtime stack.

Current behavior:

1. stop Console
2. stop Bay runtime
3. restart Bay runtime
4. restore managed agents that were running before restart
5. restart Console

During the stop phase, Downcity stops the matching process group first. ACP executors, shell sessions, and watch child processes started by an agent stop with the parent, so old children do not keep consuming memory after the restart.

## Usage

```bash
bay restart
```

## Related commands

- [`bay start`](/en/docs/commands/start)
- [`bay stop`](/en/docs/commands/stop)
- [`bay status`](/en/docs/commands/status)
- [`bay console`](/en/docs/commands/console/console)

## Example output

```text
$ bay restart
bay v1.0.0
✅ Console stopped
   pid: <consolePid>
✅ Bay runtime stopped
   pid: <studioPid>
✅ Bay runtime started
   pid: <newStudioPid>
Restarting 2 managed agents...
✅ Console started
   pid: <newConsolePid>
   url: http://127.0.0.1:5315
```
