---
title: 配置总览
description: 按层理解 Console 全局配置与 Agent 项目配置
---

# 配置总览

当前推荐配置分层：

1. Console 全局层：管理共享资源（例如模型池）。
2. Agent 项目层：声明当前项目绑定与运行参数。

## 关键原则

1. 模型定义在 Console，全局复用。
2. Agent 只声明统一的 `execution`，不重复保存 provider 细节。
3. 任何配置变更后，建议重启对应 agent 验证生效。

## 你会常改的两类文件

1. `~/.downcity/downcity.db`（通过 CLI/UI 管理，不直接手改）
2. `<project>/downcity.json`

## 下一步

1. [Console downcity.json](/zh/docs/configuration/console-downcity-json)
2. [项目配置（downcity.json）](/zh/docs/configuration/project)
3. [环境变量配置逻辑（Console 共享 vs Agent 私有）](/zh/docs/configuration/environment-variables)
4. [模型配置（Console + Agent）](/zh/docs/configuration/model)
5. [配置检查清单](/zh/docs/configuration/checklist)
