---
title: 本地 Agent 快速开始
description: 用 Agent SDK 在本地进程里创建一个可执行的 Agent
---

# 本地 Agent 快速开始

## 安装

```bash
pnpm add @downcity/agent
```

## 最小示例

```ts
import { Agent } from "@downcity/agent";
import { createOpenAI } from "@ai-sdk/openai";

const openai = createOpenAI({
  apiKey: process.env.OPENAI_API_KEY!,
});

const agent = new Agent({
  id: "repo-helper",
  path: "/path/to/project",
  tools: {},
});

const session = await agent.session();
await session.set({
  model: openai.responses("gpt-5"),
});

const result = await session.run({
  query: "总结一下当前仓库结构",
});

console.log(result.text);
```

## 最重要的三步

1. `new Agent(...)`
2. `await agent.session()`
3. `await session.set({ model })`

这三步里第 3 步最容易被遗漏，也是本地 SDK 最常见的踩坑点。

如果你漏掉第 3 步，本地 session 没有模型，无法执行。

第一次上手时，建议先不要叠加复杂 tools、services 或 HTTP 暴露，先把最短执行链跑通。

## 继续阅读

- [构造参数](/zh/docs/agent/sdk/local-agent/constructor-options)
- [Session set / run / stream](/zh/docs/agent/sdk/sessions/overview)
