---
title: Model Configuration
description: Responsibility boundary between Federation AIService model catalog and project binding
---

# Model Configuration

Model setup is split into two responsibilities:

1. City manages the model catalog and provider-side details.
2. Agent project chooses `execution.modelId` in `downcity.json`.

## Responsibility Flow

1. Use `city` to prepare or inspect the model catalog.
2. Connect Downcity to City as the local user.
3. Set `execution.modelId` in project.
4. Restart agent and validate one real invocation.

## Where commands live

Concrete workflows live in [Downcity City docs](/en/docs/reference/cli), [Downcity config docs](/en/docs/reference/cli), and [Agent restart docs](/en/docs/reference/cli).

## Failure cases

- missing `execution.modelId` target: agent startup fails.
- invalid provider/key: model invocation fails with auth/network errors.

## Related docs

- [Model Config Reference](/en/docs/reference/model-config)
