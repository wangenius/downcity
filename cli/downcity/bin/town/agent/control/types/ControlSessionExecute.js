/**
 * Control Session Execute API 类型定义。
 *
 * 关键点（中文）
 * - 统一描述 `/api/control/sessions/:sessionId/execute` 的扩展请求体。
 * - 支持通过 API 传入附件（路径或内容），由服务端落盘后注入 `<file>` 标签。
 */
export {};
//# sourceMappingURL=ControlSessionExecute.js.map