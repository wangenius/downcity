---
title: Command Environment Requirements
description: Explain required runtime environment, files, and env-variable priority for all city command groups
---

# Command Environment Requirements

This page answers one question: what environment each `city` command group needs to run correctly.

## 0) Top-level command boundary

Top-level command groups are:

- `city console`
- `city agent`
- `city service`
- `city plugin`

Commands like `city chat`, `city task`, `city memory`, `city skill`, and `city voice` are action commands under service/plugin systems, not top-level commands.

## 1) Environment by command group

| Command Group | Required Environment | Optional Environment | Notes |
| --- | --- | --- | --- |
| `city console init` | Writable `~/.downcity/` | Pre-set `LLM_API_KEY` (can be used as default init input) | Writes `~/.downcity/downcity.db` (sensitive fields encrypted) |
| `city console start/stop/restart/status/agents` | Writable `~/.downcity/console/` | None | Manages global console process and registry |
| `city console ui` | Console is running; browser can reach UI port | `--host`, `--port` | Single UI instance can switch across multiple running agents |
| `city agent create` | Writable target directory | None | Generates `downcity.json`, `PROFILE.md`, `.downcity/` |
| `city agent start` | Console is running; project has `downcity.json` + `PROFILE.md` | `--host` | Agent port is always allocated by console and then registered into console registry |
| `city agent restart/status/doctor` | Target agent is registered in console registry | None | Operates through registry resolution, not arbitrary path only |
| `city console config ...` | Valid `downcity.json` exists under target path | `--path` | Reads/writes project config; does not call runtime API |
| `city console config alias` | Writable `~/.zshrc` or `~/.bashrc` | `--shell zsh|bash` | Only writes shell alias |
| `city service ...` | Target runtime is reachable; project is registered; project has `downcity.json` + `PROFILE.md` | `--agent`, `--path`, `--host`, `--port` | `--agent` overrides `--path` |
| `city plugin ...` | Target runtime is reachable; project is registered; project has `downcity.json` + `PROFILE.md` | `--agent`, `--path`, `--host`, `--port` | `--agent` overrides `--path` |
| `city <service> <action>` (e.g. `city chat ...`) | Target runtime is reachable | `--path`, `--host`, `--port` | Forwarded through `/api/services/command` |
| `city <plugin> <action>` (e.g. `city voice ...`) | Target runtime is reachable | `--path`, `--host`, `--port` | Forwarded through `/api/plugins/action` |

## 2) Context env requirements for common commands

### `city chat ...`

- If `--chat-key` is omitted, it tries `DC_CTX_CHAT_KEY`
- If `--session-id` is omitted, it first tries `DC_SESSION_ID`
- Internal fallback still supports `DC_CTX_CONTEXT_ID`
- In agent shell, these `DC_CTX_*` variables are usually injected automatically

### `city task ...`

- Task creation/run needs `contextId`
- You can pass `--context-id` explicitly
- Or first fallback to `DC_SESSION_ID`
- Internal fallback still supports `DC_CTX_CONTEXT_ID`

### Chat channel credentials

- The only path is binding `services.chat.channels.<channel>.channelAccountId` in `downcity.json`.
- Real credentials (`botToken` / `appId` / `appSecret`) are stored in global `~/.downcity/downcity.db` (`channel_accounts`).
- `downcity.json` and agent `.env` no longer carry these credentials.

## 3) `city service` target agent resolution order

For `city service list/status/start/stop/restart/command`:

1. explicit `--agent <name>` (highest priority)
2. if `--agent` omitted and `--path` is default `.`, try `DC_AGENT_NAME` first
3. fallback to `--path` or default `.`
4. if default `.` and `DC_AGENT_PATH` exists, resolve `.` to it first

Still must satisfy:

- resolved path is registered in console registry
- resolved path contains `downcity.json` and `PROFILE.md`

## 4) Runtime API endpoint resolution order (`host/port`)

For runtime API calls (service/plugin/action commands):

1. explicit CLI args: `--host` / `--port`
2. env vars: `DC_SERVER_HOST/PORT`
3. env vars: `DC_CTX_SERVER_HOST/PORT`
4. daemon meta args (`.downcity/.debug/downcity.daemon.json`)
5. default: `127.0.0.1:5314`

Normalization rule:

- `0.0.0.0` and `::` are normalized to `127.0.0.1`

## 5) Variable injection sources

### Injected after runtime startup

- `DC_AGENT_PATH`
- `DC_AGENT_NAME`
- `DC_SERVER_HOST`
- `DC_SERVER_PORT`

### Injected into agent shell subprocess

- `DC_SESSION_ID`
- `DC_CTX_CONTEXT_ID`
- `DC_CTX_REQUEST_ID`
- `DC_CTX_SERVER_HOST`
- `DC_CTX_SERVER_PORT`

## 6) Recommended usage

For multi-agent workflows, prefer explicit target:

```bash
city service list --agent my-agent
# or
city service list --path /abs/path/to/agent
```

Inside agent shell, default form is usually enough:

```bash
city service list
```

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `packages/downcity/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ city service list --agent <agentName>
{
  "success": true,
  "services": []
}
```

```text
$ city chat send --text "hi"
{
  "success": false,
  "error": "Failed to parse command input: <error>"
}
```
