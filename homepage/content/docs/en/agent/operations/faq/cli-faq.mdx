---
title: CLI FAQ
description: Common questions about city agent commands
---

# CLI FAQ

## What is the difference between `city agent start` and `city agent start --foreground`?

- the default mode is a background daemon
- `--foreground` runs in the current terminal

## Why does `city agent chat` say it cannot connect?

Usually because the target agent is not running.

Check:

```bash
city agent status
```

If the state is not `running`, restore that first before troubleshooting chat context or prompts.
