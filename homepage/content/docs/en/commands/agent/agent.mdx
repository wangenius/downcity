---
title: sma agent
description: Manage Agent Runtime in the current directory or a target directory
---

# `sma agent`

Manage Agent Runtime start/stop/restart.

## Command hierarchy

Top-level commands are fixed to 4: `sma console`, `sma agent`, `sma service`, `sma extension`.

## Usage

```bash
sma agent <subcommand>
```

## Subcommands

- `create`: create/initialize an agent project
- `on`: start runtime
- `off`: stop runtime
- `status`: show runtime status
- `doctor`: diagnose state files and optionally fix stale pid/meta
- `restart`: restart runtime

## Subcommand docs

- [`sma agent create`](/en/docs/commands/agent/create)
- [`sma agent on`](/en/docs/commands/agent/on)
- [`sma agent off`](/en/docs/commands/agent/off)
- [`sma agent status`](/en/docs/commands/agent/status)
- [`sma agent restart`](/en/docs/commands/agent/restart)
- [`sma agent doctor`](/en/docs/commands/agent/doctor)

## Environment variable injection

On every `sma` execution, CLI resolves current agent context and injects variables into the `sma` process and its child processes:

- `SMA_AGENT_PATH`: absolute path of current agent
- `SMA_AGENT_NAME`: agent name (prefer `ship.json.name`, fallback to directory name)

> Note: this is process-scoped injection and does not mutate your parent shell session.

## Related command

- Start console first: [`sma console start`](/en/docs/commands/console/start)
