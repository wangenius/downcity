---
title: sma console agents
description: 列出当前活跃托管的 agent runtime
---

# `sma console agents`

列出当前活跃托管的 agent runtime。

## 用法

```bash
sma console agents [--json]
```

## 行为说明

- 列出当前活跃托管的 agent runtime。
- `--json` 输出结构化结果。

## 相关命令

- [`sma console`](/zh/docs/commands/console/console)
- [`sma console status`](/zh/docs/commands/console/status)
- [`sma agent`](/zh/docs/commands/agent/agent)

## 示例输出（基于当前实现）

以下示例直接对齐 `package/src` 当前命令实现（含默认输出模式与字段结构），示例值使用占位符表示。

```text
$ sma console agents --json
{
  "success": true,
  "count": 2,
  "agents": [
    {
      "projectRoot": "<projectRootA>",
      "daemonPid": 12345,
      "running": true,
      "startedAt": "<iso>",
      "updatedAt": "<iso>",
      "logPath": "<logPathA>"
    }
  ]
}
```

```text
$ sma console agents
sma version: 1.0.0
┌──────────────────────────────────────┐
│ INFO managed agents                  │
│   agents   : 0                       │
│   detail   : no running agent daemon │
└──────────────────────────────────────┘
```
