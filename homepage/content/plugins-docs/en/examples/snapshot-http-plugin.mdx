---
title: SnapshotHttpPlugin Scenario
description: Use a runtime HTTP scenario to show how a plugin can expose its own read-only endpoint
---

# SnapshotHttpPlugin Scenario

## Scenario

You want one plugin to provide a read-only snapshot endpoint:

- `GET /api/notes/snapshot`

That is a clean fit for `plugin.http.server`.

## Current boundary

This capability belongs to the full runtime HTTP assembly path.

That means:

- `plugin.http.server` is not the same kind of explicit call as `agent.plugins.runAction(...)`
- you should expose HTTP through host-side `startServer(...)`, which automatically mounts plugin HTTP routes
- if you need this, think of it as a fuller host/runtime integration capability
