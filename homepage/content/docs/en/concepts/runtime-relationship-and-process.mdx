---
title: Runtime Relationship & Process
description: Collaboration model and process order across console, agent, service, plugin, and asset
---

# Runtime Relationship & Process

## 1. Role Summary

- `console`: manages registry, model pool, shared storage, and UI
- `agent`: owns one project runtime
- `service`: owns the main workflow and lifecycle
- `plugin`: provides optional enhancement through service-defined points
- `asset`: owns dependency install and availability

In one sentence:

`console` governs, `agent` executes, `service` orchestrates, `plugin` augments, and `asset` supports dependencies.

## 2. Startup Order

1. `city console start`
2. `city agent start`
3. agent loads project config from `ship.json`
4. runtime assembles services, plugins, and assets
5. plugins register `pipeline / guard / effect / resolve` handlers on service-defined points
6. messages or tasks enter a service workflow
7. services trigger those plugin points at the right stages
8. runtime traces are written into `.ship/*`
