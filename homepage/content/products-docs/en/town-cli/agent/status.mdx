---
title: town agent status
description: Show target agent status
---

# `town agent status`

Show process status for a target agent.

Use this command before guessing whether the project is running, stopped, stale, or not initialized.

## Usage

```bash
town agent status [path]
```

## Behavior

- `path` defaults to current directory `.`
- Target agent must be registered in the managed agent registry
- Output includes agent state, process info, and key paths
- When state is `stale`, output includes `stale_reason` for direct diagnosis

## Common states

- `running`: daemon process is reachable
- `stopped`: registry exists, but no daemon is running
- `stale`: state files remain, but the underlying daemon is gone
- `not initialized`: the target directory is not yet an Agent project

If you see `stale`, run [`town agent doctor --fix`](/en/products-docs/town-cli/agent/doctor) before starting again.

## Related commands

- [`town agent start`](/en/products-docs/town-cli/agent/start)
- [`town agent restart`](/en/products-docs/town-cli/agent/restart)
- [`town agent list --running`](/en/products-docs/town-cli/agent/list)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `cli/town/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ town agent status .
town v1.0.0
┌─────────────────────────────────────────────────────────┐
│ OK town agent status                                    │
│   project    : <projectRoot>                           │
│   log        : <daemonLogPath>                         │
│   state      : running                                 │
│   pid        : <pid>                                   │
│   started_at : <isoTimestamp>                          │
└─────────────────────────────────────────────────────────┘
```

```text
$ town agent status .
town v1.0.0
┌─────────────────────────────────────────────────────────┐
│ WARN town agent status                                  │
│   project      : <projectRoot>                         │
│   log          : <daemonLogPath>                       │
│   state        : stale                                 │
│   stale_pid    : <pid>                                 │
│   stale_reason : <reason>                              │
│   fix          : town agent doctor <projectRoot> --fix  │
└─────────────────────────────────────────────────────────┘
```
