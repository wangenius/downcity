---
title: Usage
description: How to chat in the Side Panel and hand page context to a Downcity Agent
---

# Usage

## Side Panel Flow

1. Click the extension icon in the browser toolbar
2. Write a message and send it

The Side Panel uses the default Agent saved in settings and keeps a stable browser session for the current Town and Agent, so you can keep the conversation open while you browse.

The composer automatically includes the current tab as an input reference. It updates as Chrome's active tab changes, and you can remove it from the input.

When you select text on the page, a small `引用` popover appears next to the selection. Click it to insert the selected text as an inline reference inside the Side Panel composer.

## What Happens During Send

When you send a message, the extension works in this order:

1. Read the current page title and URL
2. Capture the main page body as Markdown
3. Build a Markdown attachment
4. Send it to the selected Agent

## Keyboard Shortcut

- `Cmd/Ctrl + Enter`: send the current Ask
- `Cmd + I`: open the Downcity Side Panel; if the current page has selected text, it is inserted as a reference
- `Esc`: close the Downcity Side Panel when the composer is focused
