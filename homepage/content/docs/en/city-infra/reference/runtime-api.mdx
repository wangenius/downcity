---
title: InfraRuntime Overview
description: InfraRuntime is the server runtime entry of Downcity. For details, read the newer reference entry pages directly.
---

This page is kept as a lightweight overview for the older entry path. The new reading order is:

- [Reference home](/en/docs/city-infra/reference): choose an entry by role first
- [InfraRuntime](/en/docs/city-infra/reference/infra-runtime)
- [HTTP API](/en/docs/city-infra/reference/http-api)
- [Environment variables](/en/docs/city-infra/reference/env-vars)

## What InfraRuntime is responsible for

InfraRuntime currently handles the following:

- initialize the Runtime database
- validate `user_token`
- expose a unified `/v1/*` HTTP route space
- register Service, AIService, and services through `base.use(service)`
- mount Service actions and service routes into the same `/v1/*` space
- connect usage, plans, balances, billing, logs, and risk control through hooks

If you want the concrete methods, such as:

- `use()`
- `getService()`
- `getServices()`
- `table()`
- `router()`
- `handleRequest()`

go directly to [InfraRuntime](/en/docs/city-infra/reference/infra-runtime).
