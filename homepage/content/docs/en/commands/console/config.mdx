---
title: sma console config
description: Manage project ship.json and alias (model pool is separate)
---

# `sma console config`

Manage project `ship.json` and alias.

> Naming note: configuration command is `sma console config`, not `sma config`.

## Usage

```bash
sma console config <subcommand>
```

## Subcommands

- `get [keyPath]`
- `set <keyPath> <value>`
- `unset <keyPath>`
- `alias`

> Model pool management has moved to [`sma console model`](/en/docs/commands/console/model).

## `extensions.*` path behavior

When `keyPath` starts with `extensions.`, the command automatically reads/writes console-global `~/.ship/ship.json`.

```bash
sma console config get extensions.voice
sma console config set extensions.voice.enabled true
```

## Examples

```bash
sma console config get model.primary
sma console config set model.primary fast
sma console config unset services.chat.channels.telegram
sma console config alias --dry-run
```
