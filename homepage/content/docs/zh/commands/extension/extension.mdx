---
title: sma extension
description: 管理 extension runtime 与 command 转发
---

# `sma extension`

管理 extension 运行状态，并转发 extension command。

## 命令层级

- 一级命令：`sma extension`
- 动作命令：`sma voice`

`sma voice` 属于 extension 体系动作命令，不是一级命令。

## 用法

```bash
sma extension <subcommand>
```

## 子命令

- `list`
- `status <extensionName>`
- `start <extensionName>`
- `stop <extensionName>`
- `restart <extensionName>`
- `command <extensionName> <command> --payload <json>`

## 示例

```bash
sma extension list
sma extension status voice
sma extension restart voice
sma extension command voice status --payload '{}'
```

## 说明

需要先启动 agent runtime：

```bash
sma agent start
```

## 相关动作命令

- [`sma voice`](/zh/docs/commands/extension/voice)

## 示例输出（基于当前实现）

以下示例直接对齐 `package/src` 当前命令实现（含默认输出模式与字段结构），示例值使用占位符表示。

```text
$ sma extension
sma version: 1.0.0
Usage: sma extension [options] [command]
Commands:
  list
  status <extensionName>
  start <extensionName>
  stop <extensionName>
  restart <extensionName>
  command <extensionName> <command>
```
