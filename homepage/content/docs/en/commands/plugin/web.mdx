---
title: town web
description: Choose web-access or agent-browser for the agent and verify that the current web provider is ready
---

# `town web`

`town web` is the short command group for the built-in web plugin.

> This page remains as a command entry point. For the fuller plugin structure, see [Plugin Overview](/en/agent-sdk-docs/plugins/overview), [Plugin Configuration](/en/agent-sdk-docs/plugins/configuration), and [Plugin System Text](/en/agent-sdk-docs/plugins/plugin-system).

## What it manages

- select the active web provider
- check whether the provider is ready
- inject provider-specific guidance into agent prompts
- store a small amount of provider configuration

## Common examples

```bash
# list supported providers
town web providers

# install web-access into the shared skill root (user scope by default)
town web install --provider web-access

# install into the project skill root
town web install --provider web-access --scope project

# switch to web-access
town web use web-access

# switch to agent-browser
town web use agent-browser --browser-command agent-browser

# inspect the current provider
town web doctor

# inspect status
town web status
```

## Notes

- `town web install` puts the provider skill into the shared skill roots:
  - user scope: `~/.agents/skills`
  - project scope: `PROJECT/.agents/skills`
- With `provider=web-access`, the plugin checks whether the `web-access` skill is installed locally and injects its guidance into the agent.
- With `provider=agent-browser`, the plugin checks whether the `agent-browser` CLI is runnable and injects browser-workflow guidance into the agent.
- The default provider is `web-access`.
- The external projects behind this plugin are [eze-is/web-access](https://github.com/eze-is/web-access) and `agent-browser`.

## Related

- [`town plugin`](/en/docs/commands/plugin/plugin)
- [Plugin Docs Overview](/en/agent-sdk-docs/plugins/overview)
- [`CLI Commands`](/en/docs/reference/cli)
