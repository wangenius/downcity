---
title: sma service stop
description: Stop a single service
---

# `sma service stop`

Stop a single service under a target agent.

## Usage

```bash
sma service stop <serviceName> [--agent <name> | --path <path>] [--host <host>] [--port <port>]
```

## Example

```bash
sma service stop task --agent my-agent
```

## Related commands

- [`sma service start`](/en/docs/commands/service/start)
- [`sma service status`](/en/docs/commands/service/status)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `package/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ sma service stop task
{
  "success": true,
  "service": "task"
}
```

```text
$ sma service stop task --agent missing-agent
{
  "success": false,
  "error": "Agent not found in console registry: missing-agent. Run \"sma console agents\" to inspect names."
}
```
