---
title: city service status
description: Show runtime status for a single service
---

# `city service status`

Show status of a single service under a target agent.

## Usage

```bash
city service status <serviceName> [--agent <name> | --path <path>] [--host <host>] [--port <port>]
```

## Examples

```bash
city service status chat --agent my-agent
city service status memory --path /abs/path/to/agent
```

## Related commands

- [`city service list`](/en/docs/commands/service/list)
- [`city service restart`](/en/docs/commands/service/restart)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `packages/downcity/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ city service status task
{
  "success": true,
  "service": "task"
}
```

```text
$ city service status task --path /invalid/path
{
  "success": false,
  "error": "Agent is not registered in console registry: /invalid/path. Run \"city agents\" to inspect registered agents."
}
```
