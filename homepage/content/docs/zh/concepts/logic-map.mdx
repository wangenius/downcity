---
title: 逻辑地图
description: 解释 Town runtime、Console、agent、execution context、session 与 plugins 的协作链路
---

# 逻辑地图

当前逻辑地图可以概括为：

- `town` 管理本机 runtime 与受管 agent registry
- `console` 提供浏览器控制面
- `agent` 承载一个项目 runtime
- `session` 执行一轮真实推理
- `plugins` 暴露并增强能力

## 能力地图

- 本地 plugin：`skill`、`web`、`asr`、`tts`、`chat-authorization`
- 托管 plugin：`chat`、`task`、`memory`、`contact`、`shell`、`schedule`

## 请求地图

```mermaid
sequenceDiagram
  participant Caller
  participant Plugin as managed/local plugin
  participant Context as execution context
  participant Session

  Caller->>Plugin: invoke action or runtime entry
  Plugin->>Context: read shared capability
  Plugin->>Session: enter target execution
  Session-->>Plugin: result
  Plugin-->>Caller: reply / persist / dispatch
```

## 最重要的结论

- plugin 负责能力入口与增强
- session 负责执行
- Town runtime 与 agent 负责 runtime 管理
- Console 通过 runtime API 观察和控制
