/**
 * Shell plugin options 兼容出口。
 *
 * 关键点（中文）：shell options 类型已迁移到 `@downcity/shell`。
 */

export type {
  ResolvedShellPluginOptions,
  ShellPluginOptions,
} from "@downcity/shell/types/ShellPluginOptions.js";
