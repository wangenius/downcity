/**
 * City 项目部署配置类型。
 *
 * 关键点（中文）
 * - `city.json` 是开发者手写的最小项目声明。
 * - `.env` 只保存 City 项目自身真正需要的部署输入，例如 D1 name。
 * - 配置里不放 Cloudflare token、provider key 或过细的 Worker 选项。
 */
export {};
//# sourceMappingURL=CityProjectConfig.js.map