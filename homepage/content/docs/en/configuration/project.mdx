---
title: Project Configuration (downcity.json)
description: Most-used project-level fields and practical cautions
---

# Project Configuration (downcity.json)

`downcity.json` is the project runtime entrypoint.

```json
{
  "name": "my-agent",
  "model": {
    "primary": "default"
  },
  "start": {
    "host": "127.0.0.1",
    "port": 5314
  },
  "services": {},
  "permissions": {}
}
```

## Cautions

1. `model.primary` must resolve in the global model pool.
2. `start.host/start.port` affects default CLI routing.
3. Keep project-level config in `downcity.json`, including `services`, `plugins`, and `assets`.

## Common commands

```bash
city console config get
city console config get model.primary
city console config set start.port 3100
```

## Related docs

- [downcity.json Reference](/en/docs/reference/downcity-json)
- [Quickstart Configuration](/en/docs/quickstart/configuration)
- [Environment Variable Strategy (Console Shared vs Agent Private)](/en/docs/configuration/environment-variables)
