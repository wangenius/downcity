---
title: Env 与 downcity.db 设计
description: 说明 env、渠道账户、模型池与项目 plugin 配置如何分层
---

# Env 与 downcity.db 设计

只记一个原则：

- 共享密钥和共享基础设施放 `~/.downcity/downcity.db`
- 项目行为放 `downcity.json`

## `downcity.db` 负责

- `env_entries`
- `channel_accounts`
- `model_providers`
- `models`
- 其他 console 持有的加密设置

## 项目 `downcity.json` 负责

- `model.primary`
- `services.*`
- `plugins.*`

## 为什么这么分

- 密钥不应该散落到项目配置
- plugin 开关与插件自身依赖配置属于项目行为
- console 存储不应该偷偷承载项目级 plugin 状态

## 运行时加载顺序

1. 从 `downcity.db` 读全局 env
2. 从 `downcity.db` 读 agent 私有 env
3. 读项目 `.env`
4. 读项目 `downcity.json`
5. 解析 `plugins.*`
6. 从 `downcity.db` 解析渠道凭据和模型池
