# 本地部署参数示例。
# 复制为 `.env` 后按需填写；不要把真实账号信息或密钥提交到仓库。

# Cloudflare account id。也可以通过 `city deploy . --account-id <id>` 临时传入。
CLOUDFLARE_ACCOUNT_ID=

# 部署后验证用的 Worker 地址；也可以写入 `.city/deploy.json`。
DOWNCITY_WORKER_URL=https://downcity.<your-subdomain>.workers.dev
