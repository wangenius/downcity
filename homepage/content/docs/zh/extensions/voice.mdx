---
title: Voice 扩展
description: 本地语音识别模型安装与转写
---

# Voice 扩展

Voice 扩展用于本地语音识别（STT）。

## 常用命令

```bash
sma voice models
sma voice on SenseVoiceSmall
sma voice install whisper-large-v3-turbo
sma voice use whisper-large-v3-turbo
sma voice transcribe ./demo.ogg --language zh
sma voice off
```

## 说明

- `voice on` 可交互选择模型。
- `voice install` 只安装，不改变启用状态；已存在模型会自动跳过（`--force` 强制重下）。
- 当前配置写入 console 全局 `~/.ship/ship.db` 的 `extensions_config.voice`。

## 运行态查看

```bash
sma extension status voice
```

## 相关文档

- [`sma extension`](/zh/docs/commands/extension/extension)
- [Extension Runtime](/zh/docs/concepts/extension-runtime)
