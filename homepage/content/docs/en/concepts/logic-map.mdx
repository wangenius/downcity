---
title: Architecture Logic Map
description: Concepts page 2 explaining responsibilities and interaction of console, agent, service, plugin, capability, and asset
---

# Architecture Logic Map

This page answers one thing:

what `console`, `agent`, `service`, `plugin`, `capability`, and `asset` each own, and how one request flows across them.

## 1. Responsibility Boundaries

- `console`: global control plane. Starts/stops daemons, manages registry, model pool, env, and shared storage.
- `agent`: project-scoped execution plane. Loads project config and context, owns one runtime, persists traces.
- `service`: core domain workflows such as chat/task/memory.
- `plugin`: optional enhancement modules such as voice.
- `capability`: stable invocation contract exposed by plugins.
- `asset`: installable dependency/resource used by plugins.

## 2. System Relationship

```mermaid
flowchart LR
  subgraph CP["Control Plane (Console)"]
    CLI["city CLI"] --> CONSOLE["city console start"]
    CLI --> CFG["console config / model / env"]
    CLI --> AGENTOPS["city agent ..."]
    CLI --> PLUGINOPS["city plugin ..."]
    CONSOLE --> REG["~/.ship/console/agents.json"]
  end

  subgraph DP["Execution Plane (Per Project)"]
    AGENT["agent daemon"] --> SERVICES["services"]
    AGENT --> PLUGINS["plugins"]
    PLUGINS --> CAPS["capabilities"]
    PLUGINS --> ASSETS["assets"]
    AGENT --> SHIP[".ship/*"]
  end

  CFG --> AGENT
  PLUGINOPS --> AGENT
```

## 3. Request Flow

```mermaid
sequenceDiagram
  participant User as User
  participant Channel as Telegram/Feishu/QQ
  participant Agent as agent daemon
  participant Service as service(chat/skill/task/memory)
  participant Capability as capability(audio.transcribe)
  participant Asset as asset(voice.transcriber)
  participant Disk as .ship/

  User->>Channel: Send message
  Channel->>Agent: Inbound event
  Agent->>Disk: Persist context
  Agent->>Service: Run workflow
  Service->>Capability: Invoke stable contract
  Capability->>Asset: Use installed dependency
  Asset-->>Capability: Return result
  Capability-->>Service: Return result
  Service-->>Agent: Return workflow output
  Agent->>Disk: Persist logs/cache/tasks
  Agent-->>Channel: Send reply
```

## 4. Key Rules

- `console` handles governance, `agent` handles runtime execution.
- `service` is core workflow, `plugin` is optional enhancement.
- services depend on capabilities, not plugin-private details.
- plugins depend on assets, not raw model/install logic.

## 5. Typical Command Order

```bash
city console start
city agent create .
city agent start
city service list
city plugin list
city console status
```

## Read Next

- [Architecture Overview](/en/docs/concepts/architecture)
- [Runtime Relationship & Process](/en/docs/concepts/runtime-relationship-and-process)
