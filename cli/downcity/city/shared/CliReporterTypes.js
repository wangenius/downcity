/**
 * CLI Reporter 类型定义。
 *
 * 关键点（中文）
 * - 为 city CLI 的输出排版提供统一的结构化输入。
 * - 所有命令层只描述“要表达什么”，具体排版交给 reporter 模块。
 * - 类型与渲染器放在同一子域，避免输出协议继续漂移到集中类型目录。
 */
export {};
//# sourceMappingURL=CliReporterTypes.js.map