---
title: Project Configuration (downcity.json)
description: Most-used project-level fields and practical cautions
---

# Project Configuration (downcity.json)

`downcity.json` is the project runtime entrypoint.

```json
{
  "name": "my-agent",
  "execution": {
    "type": "api",
    "modelId": "quality"
  },
  "start": {
    "host": "127.0.0.1",
    "port": 5314
  }
}
```

## Cautions

1. A project now must declare `execution`, and the mode is fixed to `api`.
2. `execution.modelId` must exist in the connected City AIService.
3. `start.host/start.port` affects default CLI routing.
4. Keep project-level config in `downcity.json`, including `plugins` and execution behavior.
5. Local shell / CLI commands now always run inside a sandbox. Top-level `sandbox` only adjusts the boundary.

## Execution Modes

If you want to use a City AIService model:

```json
{
  "execution": {
    "type": "api",
    "modelId": "default"
  }
}
```

## Shell Sandbox

In the current version, `shell` / CLI commands always run inside a sandbox. Project-level `sandbox` only adjusts the boundary:

```json
{
  "sandbox": {
    "networkMode": "off",
    "envAllowlist": ["PATH", "LANG", "TERM", "SHELL"]
  }
}
```

Current behavior:

- Affects both the shell plugin path and `kind=script` task execution
- Current backends support macOS and Linux: macOS uses built-in `sandbox-exec`; Linux uses `bubblewrap` (`bwrap`)
- Sandbox cannot be disabled and shell execution does not fall back to an unrestricted host process
- Host paths not mounted into the sandbox are not visible
- The project root and `.downcity/sandbox` are writable by default
- `writablePaths` adds project-local writable paths and must stay inside the project root
- Sandbox child processes share `.downcity/sandbox` as their persistent `HOME`, with `.downcity/sandbox/tmp` for `TMPDIR` and `.downcity/sandbox/.cache` for `XDG_CACHE_HOME`
- Project-local installs such as `.venv`, `node_modules`, and downloaded model/cache files under the project are supported
- Host-level installs such as `sudo`, `brew install`, Xcode tools, or writes to `/usr/local`, `/opt/homebrew`, and `/System` are outside the sandbox boundary
- Network is open by default; omitting the field behaves like `networkMode="full"`
- `networkMode="off"` blocks network access
- `networkMode="restricted"` is currently handled the same as open network
- `networkMode="full"` allows network access from shell commands

On Linux, install `bubblewrap` first and make sure the system allows unprivileged user namespaces. If `bwrap` is missing or user namespaces are disabled, shell commands fail instead of falling back to an unrestricted host process.

## Where to edit it

Project config can be edited directly in `downcity.json`, or through the [Town config workflow](/en/products-docs/town-cli/config) when you want a CLI surface.

## Related docs

- [downcity.json Reference](/en/docs/reference/downcity-json)
- [Configuration Overview](/en/docs/configuration/overview)
- [Environment Variable Strategy (City-side vs Agent Private)](/en/docs/configuration/environment-variables)
