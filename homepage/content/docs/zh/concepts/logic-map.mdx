---
title: 架构逻辑图
description: 解释 console、agent、execution context、session、service、plugin 的职责边界与协作链路
---

# 架构逻辑图

这页回答一个问题：

一次请求是怎么从 `console` 或 `agent` 进入真正执行，再回到用户侧的。

## 1. 职责边界

- `console`：全局控制面，管理 daemon、registry、模型池、共享 UI 状态
- `agent`：项目宿主，加载项目配置并持有 session store
- `execution context`：统一暴露给执行链路的能力面
- `session`：真正执行 prompt、tools、history 的地方
- `service`：主业务路径与领域编排
- `plugin`：在固定点被动加入的扩展模块

## 2. 系统关系

```mermaid
flowchart LR
  subgraph CP["控制面"]
    CLI["city CLI"] --> CONSOLE["console"]
    CONSOLE --> REG["registry 和 model pool 和 UI"]
  end

  subgraph DP["执行面 单项目"]
    AGENT["agent"] --> EC["execution context"]
    AGENT --> SR["session store"]
    SR --> SESSION["session"]
    EC --> SERVICES["services"]
    EC --> PLUGINS["plugins"]
    SERVICES --> SESSION
    SERVICES -. trigger plugin points .-> PLUGINS
    AGENT --> DATA[".downcity/*"]
  end

  REG --> AGENT
```

## 3. 请求流

```mermaid
sequenceDiagram
  participant User as User
  participant Channel as Telegram or Feishu or QQ
  participant Service as service
  participant Context as execution context
  participant Plugin as plugin
  participant Session as session

  User->>Channel: 发送消息或触发任务
  Channel->>Service: 进入目标 service
  Service->>Context: 读取统一能力
  Service->>Plugin: 在固定点调用扩展
  Service->>Session: 进入目标 session
  Session-->>Service: 返回结果
  Service-->>User: 回复或落盘
```

## 4. 一个贴近真实实现的例子

在 `chat` 里：

- `chat service` 先接住渠道消息
- 它解析目标 `sessionId`
- 语音输入可触发 `asr` plugin
- 鉴权和角色解析可触发 `auth` plugin
- 最终执行仍然发生在目标 `session` 内部
- 由 `chat service` 决定何时以及如何回复

## 5. 用户视角最值得记住的事

- 你平时操作的是 agent
- 真正执行的是 session
- service 是主路径
- plugin 是扩展层
- execution context 只是把这些能力统一接起来
