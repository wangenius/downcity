---
title: sma agent off
description: Stop target agent runtime
---

# `sma agent off`

Stop runtime for a target agent.

## Usage

```bash
sma agent off [path]
```

## Behavior

- `path` defaults to current directory `.`
- Target agent must be registered in console registry
- If no registry entry is found, command exits with error

## Related commands

- [`sma agent on`](/en/docs/commands/agent/on)
- [`sma agent status`](/en/docs/commands/agent/status)
- [`sma agent doctor`](/en/docs/commands/agent/doctor)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `package/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ sma agent off .
sma version: 1.0.0
✅ ShipMyAgent daemon stopped
   pid: <pid>
```

```text
$ sma agent off .
sma version: 1.0.0
ℹ️  No ShipMyAgent daemon is running (pid file not found)
```
