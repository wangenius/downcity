---
title: Configuration
description: PROFILE.md, SOUL.md, downcity.json, and runtime configuration essentials
---

# Configuration

An agent project is configured with three core files:

- `PROFILE.md`: agent role and boundaries (required)
- `SOUL.md`: stable operating principles (optional)
- `downcity.json`: runtime configuration (required)

## Key `downcity.json` fields

### `start` (advanced only)

For `town agent start`, ports are auto-allocated by Town runtime.

So in normal usage you do not need `start.port` in project `downcity.json`.

Only configure `start` when manually running foreground runtime or debugging special host binding:

```json
{
  "start": {
    "host": "0.0.0.0"
  }
}
```

### `execution`

Agent model binding:

```json
{
  "execution": {
    "type": "api",
    "modelId": "quality"
  }
}
```

Notes:

- project runtime now binds through `execution.modelId`
- `modelId` should match a model exposed by the connected City AIService
- for the current project runtime path, `execution.type` should be `api`
- provider secrets and provider connection details stay outside the project config

### `plugins.chat`

Chat channels and queue behavior:

```json
{
  "plugins": {
    "chat": {
      "queue": {
        "maxConcurrency": 2,
        "mergeDebounceMs": 600,
        "mergeMaxWaitMs": 2000
      },
      "channels": {
        "telegram": {
          "enabled": true,
          "channelAccountId": "telegram-main"
        }
      }
    }
  }
}
```

`channelAccountId` binds to the global channel account registry in `~/.downcity/downcity.db` (`channel_accounts`).

`plugins.chat.queue` notes:
- `maxConcurrency`: max lane concurrency across different chat keys.
- `mergeDebounceMs`: short debounce window for batching consecutive inbound messages in one run.
- `mergeMaxWaitMs`: hard cap for the batching wait to avoid unbounded delay.

### `plugins.skill`

Skills discovery roots:

```json
{
  "plugins": {
    "skill": {
      "enabled": true,
      "paths": [".agents/skills"],
      "allowExternalPaths": false
    }
  }
}
```

### `plugins.*`

Plugin config is maintained in project `downcity.json`.

Example for asr:

```bash
town asr on SenseVoiceSmall
town config get plugins.asr
```

## Useful commands

```bash
town config get
town config get execution.modelId
town city status
town config unset plugins.chat.channels.telegram
```

## Next

- [Agent Configuration Overview](/en/docs/agent/configuration/overview)
- [CLI Reference](/en/docs/reference/cli)
- [`.downcity/` Directory](/en/docs/reference/dot-downcity)
- [Environment Variable Strategy (City-side vs Agent Private)](/en/docs/configuration/environment-variables)
