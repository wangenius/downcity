---
title: Env and downcity.db Design
description: Explain how env, channel accounts, model pool, and project plugin config are separated
---

# Env and downcity.db Design

Use one rule:

- shared secrets and shared infrastructure live in `~/.downcity/downcity.db`
- project behavior lives in `downcity.json`

## `downcity.db` owns

- `env_entries`
- `channel_accounts`
- `model_providers`
- `models`
- other console-owned encrypted settings

## Project `downcity.json` owns

- `model.primary`
- `services.*`
- `plugins.*`
- `assets.*`

## Why

- secrets should not be duplicated into project config
- plugin enablement and asset selection are project behavior
- console storage should not hide project-specific plugin state

## Runtime load order

1. read global env from `downcity.db`
2. read agent-private env from `downcity.db`
3. read project `.env`
4. read project `downcity.json`
5. resolve `plugins.*` and `assets.*`
6. resolve channel credentials and model pool from `downcity.db`
