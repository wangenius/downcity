---
title: Current Limitations
description: Capability boundaries and limitations to know when using RemoteAgent
---

# Current Limitations

If the server exposes neither:

- HTTP through `startServer(...)`

nor:

- RPC through `agent.start({ rpc: ... })`

then `RemoteAgent` cannot connect.
