---
title: Console Config Storage (downcity.db)
description: Console-global config is now unified in ~/.downcity/downcity.db with encrypted sensitive fields
---

# Console Config Storage (downcity.db)

From current versions, Console no longer uses:

1. `~/.downcity/downcity.json`
2. `~/.downcity/.env`

Console-global config is unified in:

- `~/.downcity/downcity.db`

## What is stored

`downcity.db` contains these major groups:

1. Model pool (providers/models)
2. Console-global env (`global_env`)
3. Agent-private env (`agent_env`)
4. Bot account vault (`channel_accounts`)
5. Project plugin config (`plugins.*`, `assets.*`)

Sensitive fields (such as API keys) are stored encrypted.

## Boundary with Agent project config

These files remain agent-project-level config (unchanged):

1. `<agent>/downcity.json`
2. `<agent>/.env`

So the split is:

- Console: `~/.downcity/downcity.db`
- Agent: project-local `downcity.json/.env`

## Environment Variable Scope (Important)

Current versions use explicit layers:

1. Console-global shared env: `global_env`
2. Agent-private env in DB: `agent_env`
3. Agent-local runtime overlay: `<agent>/.env` (user-managed)
4. Chat channel credentials: `channel_accounts` (bound by `downcity.json` `channelAccountId`)

Key behavior:

1. Runtime env map is `agent_env + <agent>/.env` (`.env` wins).
2. `.env` remains agent-local and does not mutate global `process.env`.
3. `downcity.json` stores channel binding (`channelAccountId`) instead of plaintext secrets.
4. Bot credentials are resolved from `channel_accounts`.
5. Plugin config is read from project `downcity.json`.

## Common operations

Initialize Console (writes into `downcity.db`):

```bash
city console init
```

Read/update project plugin config:

```bash
city console config get plugins.voice
city console config set plugins.voice.enabled true
```

Manage model pool:

```bash
city console model list
city console model create
```

## Related docs

- [Env and downcity.db Database Design](/en/docs/configuration/env-downcitydb-design)
- [Environment Variable Strategy (Console Shared vs Agent Private)](/en/docs/configuration/environment-variables)

## Runtime files

These runtime files stay in filesystem (not config):

1. `~/.downcity/console/console.pid`
2. `~/.downcity/console/ui.pid`
3. `~/.downcity/console/console.log`
4. `~/.downcity/console/ui.log`
5. `~/.downcity/console/agents.json`
