---
title: Agent SDK
description: Use Downcity as a local embeddable Agent or as a remote Agent client
---

# Agent SDK

The full Agent SDK documentation now lives inside the dedicated Agent docs domain.

Start here:

- [Agent SDK Overview](/en/agent-sdk-docs)
- [Local Agent Quickstart](/en/agent-sdk-docs/local-agent/quickstart)
- [RemoteAgent Quickstart](/en/agent-sdk-docs/remote-agent/quickstart)
- [Session Overview](/en/agent-sdk-docs/sessions/overview)

---

The `@downcity/agent` package now exposes two primary SDK entries:

- `Agent`: local embeddable agent runtime
- `RemoteAgent`: remote HTTP client for an already-running agent

## Install

```bash
pnpm add @downcity/agent
```

If you want to use built-in plugin classes such as `ChatPlugin`, install the built-in plugin package too:

```bash
pnpm add @downcity/plugins
```

## Local Agent

Use `new Agent(...)` when your app runs in the same process and can provide tools, plugins, and models directly.

```ts
import { Agent } from "@downcity/agent";
import { createOpenAI } from "@ai-sdk/openai";

const openai = createOpenAI({
  apiKey: process.env.OPENAI_API_KEY!,
});

const agent = new Agent({
  id: "vibecape-official",
  path: "/path/to/agent-project",
  tools: {},
});

const session = await agent.createSession();
await session.set({
  model: openai.responses("gpt-5"),
});

const turn = await session.prompt({
  query: "Rename the local file to match the new convention",
});
const result = await turn.finished;

console.log(result.text);
```

## Inject ChatPlugin

If your client needs to start chat channels directly inside `@downcity/agent`, pass an explicit `ChatPlugin` instance:

```ts
import { Agent } from "@downcity/agent";
import { ChatPlugin } from "@downcity/plugins";

const agent = new Agent({
  id: "vibecape-official",
  path: "/path/to/agent-project",
  tools: {},
  plugins: [
    new ChatPlugin({
      telegram: {
        botToken: process.env.TELEGRAM_BOT_TOKEN!,
      },
    }),
  ],
});
```

`plugins` accepts already-instantiated plugin objects.  
`agent.start({ http: { ... }, rpc: true })` automatically starts those plugins before exposing the endpoints.

## Observe a session

```ts
const unsubscribe = session.subscribe((event) => {
  if (event.type === "text-delta") {
    process.stdout.write(event.text);
  }
});

const turn = await session.prompt({
  query: "Continue the previous task",
});
await turn.finished;
unsubscribe();
```

Current session events include:

- `text-delta`
- `reasoning-delta`
- `tool-call`
- `tool-result`
- `error`

## Fork a session

```ts
const branched = await session.fork();
const fromMessage = await session.fork("message-id");
```

If `messageId` is provided, the new session copies history up to and including that message.

## Start HTTP / RPC

```ts
const started = await agent.start({
  http: {
    host: "127.0.0.1",
    port: 15314,
  },
  rpc: true,
});

console.log(started.http?.baseUrl);
```

## Remote Agent

Use `RemoteAgent` when another process has already started `agent.start({ http: { ... } })`.

```ts
import { RemoteAgent } from "@downcity/agent";

const agent = new RemoteAgent({
  baseUrl: "http://127.0.0.1:15314",
});

const session = await agent.createSession();

const turn = await session.prompt({
  query: "Summarize the repository structure",
});
const result = await turn.finished;

console.log(result.text);
```

Remote observation uses the same API:

```ts
const unsubscribe = session.subscribe((event) => {
  if (event.type === "text-delta") {
    process.stdout.write(event.text);
  }
});

const turn = await session.prompt({ query: "Continue" });
await turn.finished;
unsubscribe();
```

## Session storage

SDK sessions are stored under:

```text
<projectRoot>/.downcity/agents/<agentId>/sessions/<sessionId>/messages/messages.jsonl
```

This keeps sessions from different agents isolated even when they share the same project directory.
