---
title: CLI Commands
description: Command-line reference entry for Downcity
---

# CLI Commands

CLI docs are organized by command pages and command groups.

## Command docs directory

- Open the `Commands` section and pick a command or group directly.
- Both fixed commands and dynamically registered commands are listed there.

## Primary command groups

- Fixed root commands:
  - [`city init`](/en/docs/commands/init)
  - [`city start`](/en/docs/commands/start)
  - [`city stop`](/en/docs/commands/stop)
  - [`city restart`](/en/docs/commands/restart)
  - [`city update`](/en/docs/commands/update)
  - [`city status`](/en/docs/commands/status)
  - [`city public`](/en/docs/commands/public)
- Fixed module commands:
  - [`city console`](/en/docs/commands/console/console)
  - [`city agent`](/en/docs/commands/agent/agent)
  - [`city agent create`](/en/docs/commands/agent/create)
  - [`city agent list`](/en/docs/commands/agent/list)
  - [`city agent chat`](/en/docs/commands/agent/chat)
  - [`city config`](/en/docs/commands/config)
  - [`city model`](/en/docs/commands/model)
  - [`city env`](/en/docs/commands/env)
  - [`city plugin`](/en/docs/commands/plugin/plugin)
- Dynamic action commands:
  - [`city chat`](/en/docs/commands/plugin/chat)
  - [`city task`](/en/docs/commands/plugin/task)
  - [`city memory`](/en/docs/commands/plugin/memory)
  - [`city contact`](/en/docs/commands/plugin/contact)
  - [`city shell`](/en/docs/commands/plugin/shell)
  - [`city skill`](/en/docs/commands/plugin/skill)
  - [`city web`](/en/docs/commands/plugin/web)
  - [`city asr`](/en/docs/commands/plugin/asr)
  - [`city tts`](/en/docs/commands/plugin/tts)

## Runtime model (short)

- `city start`: start city runtime only
- `city start --console` / `city start -a`: start city runtime and Console together
- `city console start`: start Console only, after city runtime is already running
- `city stop`: stop Console, then stop city runtime and managed agents
- `city restart`: restart city runtime, restore managed agents, then restart Console
- `city update`: update the currently installed global Downcity CLI package
- `city public`: manage persisted public exposure for Console, with interactive mode plus `on/off/status`
- `city agent`: open the interactive agent manager in a TTY
- `city agent create`: initialize project runtime files
- `city agent list`: inspect registered agents, or use `--running` for active ones only
- `city agent start`: start project agent daemon
- `city agent stop`: stop project agent daemon
- `city agent chat`: chat with an agent in interactive mode or send one-shot messages with `--message`
- `city console`: manage the Console module itself
- `city plugin ...`: inspect plugin catalog, manage plugin lifecycle, or use advanced plugin command forms
- `city chat/task/memory/contact/shell ...`: normal managed-plugin runtime commands
- `city web/skill/asr/tts ...`: normal local plugin commands
- `city model`: open the interactive global model manager in a TTY
- `city model ...`: manage model config for runtime and automation

## Always up to date

Use local help for latest flags:

```bash
city --help
city <command> --help
```
