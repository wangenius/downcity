---
title: city agent
description: Manage the agent process in the current directory or a target directory
---

# `city agent`

Manage agent lifecycle and registry-aware operations.

## Command hierarchy

Top-level command groups are: `city console`, `city agent`, `city service`, `city plugin`.

## Usage

```bash
city agent <subcommand>
```

## Subcommands

- `create`: create/initialize an agent project
- `list`: list registered agent projects
- `start`: start the agent
- `status`: show agent status
- `doctor`: diagnose state files and optionally fix stale pid/meta
- `restart`: restart the agent

## Subcommand docs

- [`city agent create`](/en/docs/commands/agent/create)
- [`city agent list`](/en/docs/commands/agent/list)
- [`city agent start`](/en/docs/commands/agent/start)
- [`city agent status`](/en/docs/commands/agent/status)
- [`city agent restart`](/en/docs/commands/agent/restart)
- [`city agent doctor`](/en/docs/commands/agent/doctor)

## Environment variable injection

On every `city` execution, CLI resolves current agent context and injects variables into the `city` process and its child processes:

- `DC_AGENT_PATH`: absolute path of current agent
- `DC_AGENT_NAME`: agent name (prefer `downcity.json.name`, fallback to directory name)

> Note: this is process-scoped injection and does not mutate your parent shell session.

## Related command

- Start console first: [`city start`](/en/docs/commands/start)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `packages/downcity/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ city agent
city version: 1.0.0
Usage: city agent [options] [command]
Commands:
  create [path]
  list
  start [path]
  status [path]
  doctor [path]
  restart [path]
```
