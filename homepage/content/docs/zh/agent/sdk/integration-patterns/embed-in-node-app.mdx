---
title: 嵌入到 Node 应用
description: 在 Node.js 应用里直接创建和使用本地 Agent 的推荐模式
---

# 嵌入到 Node 应用

这是最自然的 SDK 使用方式：

- 你的应用代码直接创建 `Agent`
- 应用启动时准备 session
- handler 收到请求后转成 `run()` 或 `stream()`

适合：

- 内部工具
- MCP 风格包装层
- 面向项目的研发助手服务

这种模式最大的优点是：

- 上下文最完整
- 调试最直接
- 不需要先跨进程排查网络问题
