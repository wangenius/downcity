/**
 * Daemon API 协议类型模块。
 *
 * 关键点（中文）
 * - 这里描述的是 CLI 与 agent daemon 之间的远程访问契约，不包含具体 HTTP 实现。
 * - plugin runtime 等上层模块只依赖这些协议类型，不需要感知 Town/daemon 的内部实现细节。
 * - 该文件属于 `main/town/daemon/*` 的协议边界，而不是通用 HTTP 类型目录。
 */
export {};
//# sourceMappingURL=Api.js.map