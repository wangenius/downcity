```mermaid
flowchart TD
  subgraph UI["Console UI"]
    UI1["AuthorizationSection.tsx<br/>权限分组管理页"]
    UI2["useConsoleDashboard.ts<br/>调用后端接口"]
    UI3["Dashboard.ts<br/>UI 类型定义"]
  end

  subgraph API["Console / API"]
    API1["TuiApi.ts<br/>/api/tui/authorization"]
    API2["ChannelAccountApiRoutes.ts<br/>channel account 接口"]
    API3["ChannelAccountService.ts<br/>channel account 服务"]
  end

  subgraph Types["权限领域类型"]
    T1["types/Authorization.ts<br/>role / permission / config / result"]
  end

  subgraph Runtime["权限运行时"]
    R1["AuthorizationConfig.ts<br/>ship.db 读写 / 归一化"]
    R2["AuthorizationPolicy.ts<br/>权限判定"]
    R3["AuthorizationStore.ts<br/>观测 user/chat"]
    R4["QueuedUserMessage.ts<br/>写入 role_id / permissions"]
    R5["ChatQueue.ts<br/>执行队列"]
    R6["ChatHistoryStore.ts<br/>入站/出站 history"]
    R7["ChatMetaStore.ts<br/>context/chat 元信息"]
  end

  subgraph Channel["平台适配层"]
    C1["BaseChatChannel.ts<br/>统一接入层"]
    C2["telegram/Bot.ts"]
    C3["feishu/Feishu.ts"]
    C4["qq/QQ.ts"]
  end

  subgraph Store["存储层"]
    S1["utils/store/index.ts<br/>SQLite 读写"]
    S2["utils/store/schema.ts<br/>表结构定义"]
  end

  UI1 --> UI2
  UI2 --> API1
  UI1 --> UI3

  API1 --> R1
  API1 --> R3

  API2 --> API3
  API3 --> S1
  S1 --> S2

  R1 --> S1
  R1 --> T1
  R2 --> R1
  R2 --> T1
  R3 --> R1
  R4 --> T1

  C2 --> C1
  C3 --> C1
  C4 --> C1

  C1 --> R2
  C1 --> R3
  C1 --> R4
  C1 --> R5
  C1 --> R6
  C1 --> R7
```

**调用顺序图**

```mermaid
sequenceDiagram
  participant P as Platform Message
  participant A as Channel Adapter
  participant B as BaseChatChannel
  participant S as AuthorizationStore
  participant C as AuthorizationConfig
  participant O as AuthorizationPolicy
  participant Q as QueuedUserMessage
  participant K as ChatQueue

  P->>A: 原始消息
  A->>A: 提取 chatId/chatType/userId/username
  A->>B: observeIncomingAuthorization(...)
  B->>S: 记录观测到的 user/chat

  A->>B: evaluateIncomingAuthorization(...)
  B->>C: 读取 ship.db 权限配置
  C-->>B: roles + channel.userRoles + defaultUserRoleId
  B->>O: 判定 allow/block
  O-->>B: decision + userRoleId + userPermissions

  alt allow
    A->>B: enqueueMessage(...)
    B->>Q: 生成 <info> 头
    Q-->>B: role_id + permissions + text
    B->>K: 入执行队列
  else block
    A->>A: 私聊可回复 blocked 提示
  end
```

**按目录理解**

```text
package/src/services/chat/
├── types/
│   └── Authorization.ts
│      只定义权限领域模型
│
├── runtime/
│   ├── AuthorizationConfig.ts
│   │  只负责读写 ship.db 权限配置
│   ├── AuthorizationPolicy.ts
│   │  只负责判定 allow / block
│   ├── AuthorizationStore.ts
│   │  只负责记录观测到的 user/chat
│   └── QueuedUserMessage.ts
│      只负责把权限信息写进入队消息
│
└── channels/
   ├── BaseChatChannel.ts
   │  统一串起“观测 -> 判定 -> 入队”
   ├── telegram/Bot.ts
   ├── feishu/Feishu.ts
   └── qq/QQ.ts
      各平台只负责提取平台字段并接入统一链路
```

**一句话版**

- `Authorization.ts` 定义“权限长什么样”
- `AuthorizationConfig.ts` 管“权限配在哪里”
- `AuthorizationPolicy.ts` 管“怎么判”
- `AuthorizationStore.ts` 管“看到了谁”
- `BaseChatChannel.ts` 管“把消息接进来后怎么走”
- 各平台 adapter 只负责“把平台消息翻译成统一输入”

如果你要，我下一步可以再给你画一张“ship.db 里权限数据结构图”。