---
title: town restart
description: Restart Town runtime, restore managed agents, then restart Console
---

# `town restart`

Restart the full Town runtime stack.

Current behavior:

1. stop Console
2. stop Town runtime
3. restart Town runtime
4. restore managed agents that were running before restart
5. restart Console

During the stop phase, Downcity stops the matching process group first. ACP executors, shell sessions, and watch child processes started by an agent stop with the parent, so old children do not keep consuming memory after the restart.

## Usage

```bash
town restart
```

## Related commands

- [`town start`](/en/docs/commands/start)
- [`town stop`](/en/docs/commands/stop)
- [`town status`](/en/docs/commands/status)
- [`town console`](/en/docs/commands/console/console)

## Example output

```text
$ town restart
town v1.0.0
✅ Console stopped
   pid: <consolePid>
✅ Town runtime stopped
   pid: <bayPid>
✅ Town runtime started
   pid: <newBayPid>
Restarting 2 managed agents...
✅ Console started
   pid: <newConsolePid>
   url: http://127.0.0.1:5315
```
