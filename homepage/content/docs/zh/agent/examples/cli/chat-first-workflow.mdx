---
title: 例子：先聊天再迭代配置
description: 用最小配置启动 Agent，然后通过持续聊天逐步打磨它
---

# 例子：先聊天再迭代配置

如果你还不确定最终要把 agent 做成什么样，推荐先走这条路径：

1. 初始化
2. 绑定一个模型
3. 启动 agent
4. 通过 `bay agent chat` 连续对话
5. 再回头调整 `PROFILE.md` 和 `SOUL.md`

这条路径特别适合还不确定最终规则结构，但想先建立使用直觉的场景。
