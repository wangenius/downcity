#!/usr/bin/env node
/**
 * downfed CLI 根命令装配模块。
 *
 * 关键点（中文）
 * - `downfed` 是 Federation 管理器，负责部署、City 实体、服务资源等 admin 能力。
 * - 默认无参数时打开交互式 Federation 管理界面。
 * - 本模块承载 commander 根命令，`src/index.ts` 只负责按命令名分发。
 */
/**
 * 执行 downfed CLI。
 */
export declare function runDownfedCli(): Promise<void>;
//# sourceMappingURL=downfed.d.ts.map