---
title: sma service start
description: Start a single service
---

# `sma service start`

Start a single service under a target agent.

## Usage

```bash
sma service start <serviceName> [--agent <name> | --path <path>] [--host <host>] [--port <port>]
```

## Example

```bash
sma service start task --agent my-agent
```

## Related commands

- [`sma service stop`](/en/docs/commands/service/stop)
- [`sma service restart`](/en/docs/commands/service/restart)

## Example Output (Based on Current Implementation)

The examples below are aligned with current command implementations in `package/src` (including default output mode and field structure). Placeholder values are used for environment-specific fields.

```text
$ sma service start task
{
  "success": true,
  "service": "task"
}
```

```text
$ sma service start task --agent missing-agent
{
  "success": false,
  "error": "Agent not found in console registry: missing-agent. Run \"sma console agents\" to inspect names."
}
```
