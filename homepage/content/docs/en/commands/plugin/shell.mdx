---
title: town shell
description: Managed shell plugin commands for runtime shell execution and session ownership
---

# `town shell`

`town shell` is the managed shell plugin command group.

Use it when you want shell execution that belongs to the agent runtime instead of an ad-hoc local terminal step.

## What this plugin shape means

- shell session ownership belongs to the managed shell plugin runtime
- start/stop cleanup belongs to plugin lifecycle
- command execution and session state stay tied to the target agent

## Recommended path

- inspect lifecycle and boundaries in [Plugins Docs / shell](/en/plugins-docs/builtins/shell)
- use `town plugin status shell`, `town plugin start shell`, or `town plugin restart shell` when you need lifecycle-level control

## Related

- [`town plugin`](/en/docs/commands/plugin/plugin)
- [`Plugins Docs / shell`](/en/plugins-docs/builtins/shell)
