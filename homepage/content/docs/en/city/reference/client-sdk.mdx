---
title: SDK Overview
description: "The Downcity SDK is split by role: User Visa owns bay-runtime calls, while Admin Visa owns trusted-side management calls."
---

This page does one job: it pulls the calling model of `@downcity/city` into one reference overview.

If you want the integration story first, start with [@downcity/city](/en/docs/city/packages/city/visa). If you need concrete method details, continue to [User Visa](/en/docs/city/reference/visa-user) and [Admin Visa](/en/docs/city/reference/visa-admin).

## Two roles

| | User Visa | Admin Visa |
|---|---|---|
| Used by | frontend, app, extension, desktop client | bay backend, ops scripts, internal tools |
| Owns | AI services, custom services, public services | bays, tokens, runtime env, admin-side services |
| Common auth | `user_token` | `admin_secret_key` |
| Safe in browser | yes | no |

## Four common call types

```ts
// Bay side
const client = new Visa({
  role: "user",
  city_url,
  bay_id,
  user_token,
});

// AI Service
await client.ai.text({ prompt: "hello", model: "deepseek-v4-flash" });
await client.ai.stream({ prompt: "hello" });

// Custom Service
await client.service("translate").action("zh2en").invoke({ text: "hello" });
await client.service("accounts").action("login").invoke({ email: "a@b.com", password: "..." });

// GET action
const items = await client.service("bays").get("list");

// Payment methods
const methods = await client.payment.methods();
const checkout = await client.payment.method("stripe").invoke({
  topup_id: "topup_demo",
});

// Admin side
const admin = new Visa({
  role: "admin",
  city_url,
  admin_secret_key,
});
await admin.service("bays").action("create").invoke({ name: "My App" });
```

## Relationship between service, service, and AI service

- AI service: called through `client.ai.*`
- custom service: called through `client.service(name)...`
- service: still called through `client.service(name)...`, but the service comes from a service

Examples:

- `rewrite`, `translate`: custom services you registered yourself
- `accounts`, `usage`, `payment`, `payment.stripe`: services exposed by official packages

From the caller's point of view, they all live in the same City service route space.

## Where registration and login fit in the SDK model

The two most common paths are:

1. your backend finishes login first, then uses `Admin Visa` to request `user_token`
2. the frontend uses a guest `User Visa` to call `accounts.login/register`, and the accounts service returns `user_token`

Both paths end in the same user context:

```txt
city_url + bay_id + user_token
  -> new Visa({ role: "user", ... })
  -> client.ai.* or client.service(...)
```

## Which page to read next

1. To understand package composition, read [Packages](/en/docs/city/packages)
2. To understand bay integration, read [@downcity/city](/en/docs/city/packages/city/visa)
3. For detailed bay-side calls, read [User Visa](/en/docs/city/reference/visa-user)
4. For trusted-side management, read [Admin Visa](/en/docs/city/reference/visa-admin)
5. For minimal auth, read [@downcity/services](/en/docs/city/packages/services) and [Accounts Service](/en/docs/city/guides/accounts-service)
