---
title: Configuration Checklist
description: Production/readiness checks before rollout
---

# Configuration Checklist

Use this checklist before release:

1. Model binding
- Verify `downcity.json.model.primary` exists in `city model list`.

2. Routing and network
- Verify `start.host/start.port` match runtime daemon.
- Ensure no stale `DC_SERVER_*` env values.

3. Channels and services
- Enable only required channels/services.
- Run one send/receive test per enabled channel.

4. Security and secrets
- Ensure `DC_MODEL_DB_KEY` is injected securely.
- Verify chat authorization groups and channel-account bindings are correct.

5. Observability
- Confirm log paths are writable and traceable.
- Check `city status` and `city agent status` are healthy.

## Related docs

- [Security and Permissions Overview](/en/docs/security/overview)
- [Operations](/en/docs/operations)
