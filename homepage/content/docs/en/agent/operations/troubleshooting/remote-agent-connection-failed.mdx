---
title: RemoteAgent Connection Failed
description: Troubleshooting order when RemoteAgent cannot connect to the server
---

# RemoteAgent Connection Failed

Check these first:

1. whether the server exposed HTTP through Town or started RPC through `agent.start({ rpc: ... })`
2. whether `url` is correct
3. whether the server port is really listening

## Common misunderstanding

Creating a `RemoteAgent` client does not mean the server will start automatically.

The server must explicitly expose HTTP or RPC first.

So the first debugging target here is usually not the session. It is:

- whether the server is actually listening
- whether `url` points to the right place
