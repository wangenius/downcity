/**
 * Sandbox 类型兼容出口。
 *
 * 关键点（中文）：sandbox 类型已迁移到 `@downcity/shell`。
 */

export type {
  AgentSandboxConfigRecord,
  SandboxConfig,
  SandboxNetworkMode,
  SandboxPathAccessMode,
  SandboxPathRule,
  SandboxProjectConfig,
} from "@downcity/shell/sandbox/types/Sandbox.js";
