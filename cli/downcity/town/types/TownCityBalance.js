/**
 * Town City user 余额与充值类型。
 *
 * 关键点（中文）
 * - 这些类型只描述 Town CLI 用户侧展示与调用结果。
 * - `balance` 是 microcredits 整数；`balance_microcredits` 是同值的显式说明字段。
 * - 真实余额账户、充值单与支付记录仍由 City 的 balance/payment 服务定义。
 */
export {};
//# sourceMappingURL=TownCityBalance.js.map