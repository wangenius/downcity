---
title: Downcity Infra 文档
description: 一个 InfraRuntime，让多个 AI 产品不再重复搭后端。
---

Downcity 是一个给 AI 产品准备的共用 AI infrastructure runtime，也是一个面向多个产品复用的共用后端底座。

你先部署一个自己的 InfraRuntime。以后做网页、扩展、App、桌面工具、客户 Demo 或内部工具时，都可以直接接入它，不用每个 AI 产品都重搭一套 AI 后端。

它对外暴露的不是 provider API，而是**产品调用边界**：

- 产品侧拿到 `product_id + user_token`
- 用 `UserClient` 调 `text()`、`stream()`、`image()` 这类 service
- InfraRuntime 内部自己决定模型目录、provider 路由、hooks、usage 和 env

## 先选你的阅读入口

<div className="dc-doc-grid">
  <a className="dc-doc-card" href="/docs/city-infra/quickstart">
    <em>Start Here</em>
    <strong>我想先跑通</strong>
    <p>最短路径部署一个 InfraRuntime，写入模型，签发 token，让产品端成功调到 `text()` 或 `stream()`。</p>
  </a>
  <a className="dc-doc-card" href="/docs/city-infra/concepts">
    <em>Mental Model</em>
    <strong>我想先理解架构</strong>
    <p>先分清 InfraRuntime、产品前端、业务后端、AI Gateway 和真实 provider 分别负责什么。</p>
  </a>
  <a className="dc-doc-card" href="/docs/city-infra/guides">
    <em>Real Scenarios</em>
    <strong>我已经在接真实产品</strong>
    <p>看纯客户端产品、多产品复用、鉴权、provider env、hooks 和商业逻辑的真实使用方式。</p>
  </a>
  <a className="dc-doc-card" href="/docs/city-infra/packages">
    <em>Packages</em>
    <strong>我想按包来查</strong>
    <p>直接按 `@downcity/infra`、`client` 和官方服务来理解整个 package 体系。</p>
  </a>
  <a className="dc-doc-card" href="/docs/city-infra/reference">
    <em>API</em>
    <strong>我只想查接口</strong>
    <p>按角色直接看 InfraRuntime、UserClient、AdminClient、HTTP API 和环境变量说明。</p>
  </a>
</div>

## 一张图看懂它怎么工作

<div className="dc-doc-flow" aria-label="Downcity 的整体调用链">
  <div className="dc-doc-lane">
    <div className="dc-doc-node">
      <strong>多个产品 client</strong>
      <span>网页、扩展、App、桌面工具、客户 Demo、内部工具。</span>
      <code>UserClient + product_id + user_token</code>
    </div>
  </div>
  <div className="dc-doc-arrow" aria-hidden="true">→</div>
  <div className="dc-doc-lane">
    <div className="dc-doc-node">
      <strong>Downcity InfraRuntime</strong>
      <span>校验 token，读取模型目录，再把请求分发到 AIService、服务或自定义 Service 的 action。</span>
    </div>
    <div className="dc-doc-node">
      <strong>hooks 与 usage</strong>
      <span>把套餐、余额、扣费、日志、风控接进 InfraRuntime，但业务规则仍然留在你自己的系统里。</span>
    </div>
  </div>
  <div className="dc-doc-arrow" aria-hidden="true">→</div>
  <div className="dc-doc-lane">
    <div className="dc-doc-node">
      <strong>上游调用层</strong>
      <span>OpenAI、Claude、Gemini、私有模型，或你自己的 AI Gateway / 中转服务。</span>
    </div>
  </div>
</div>

<p className="dc-doc-caption">
  对产品来说，InfraRuntime 暴露的是稳定的 service 和 model；上游接什么 provider、网关或私有部署，都是 InfraRuntime 内部细节。
</p>

## InfraRuntime 负责什么

- Runtime 数据库和 Service 声明的表
- provider key 和 Runtime env
- `user_token` 校验与 `admin_secret_key` 管理能力
- `Provider` + `AIService.use()` 注册模型，自动提供 SDK 和 OpenAI 兼容两条通路
- `client.ai.listModels()` 读取 Runtime 注册出来的公开模型目录
- usage 记录和 hooks 扩展点

## InfraRuntime 不负责什么

- 你的用户系统
- 订单、订阅、CRM、内容数据
- 产品自己的页面、交互和业务体验
- 对外暴露“统一 provider API”给其他系统直接调用

更准确地说：

> InfraRuntime 负责 AI 调用这层共用基础设施。  
> 产品继续负责自己的用户、业务、界面和商业模式。

## 推荐阅读路线

1. 第一次接入，先去 [快速开始](/docs/city-infra/quickstart)
2. 想彻底理解这个产品模型，再看 [理解 Downcity Infra](/docs/city-infra/concepts)
3. 准备接真实业务，再看 [场景指南](/docs/city-infra/guides)
4. 想按 npm package 理清职责时，看 [Packages](/docs/city-infra/packages)
5. 需要具体接口时，再回到 [参考](/docs/city-infra/reference)
