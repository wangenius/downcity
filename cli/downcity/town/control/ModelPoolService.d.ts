/**
 * 兼容导出 `ModelPoolService`。
 *
 * 关键点（中文）
 * - 真实实现已下沉到 `src/model/service/ModelPoolService.ts`。
 * - 这里保留薄转发，避免内部旧引用在渐进调整时失效。
 */
export { ModelPoolService } from "../model/service/ModelPoolService.js";
//# sourceMappingURL=ModelPoolService.d.ts.map