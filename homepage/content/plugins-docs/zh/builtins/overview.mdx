---
title: Built-ins Overview
description: Downcity 当前全部内建插件的完整地图，包括公开 SDK 插件和平台内部基础设施插件
---

# Built-ins Overview

当前代码里的内建 plugin 能力包括：

- `skill`
- `web`
- `asr`
- `tts`
- `workboard`
- `chat`
- `contact`
- `schedule`
- `task`
- `memory`
- `shell`

## 这里有两个重要分组

### 面向 SDK 用户的公开能力型 built-ins

这组通常最值得普通开发者先读：

- `skill`
- `web`
- `asr`
- `tts`
- `workboard`
- `chat`

其中 `asr` 和 `tts` 需要通过 constructor 注入真实 AI 能力，因此它们是内建可用 class，但不在默认无参创建集合里。

它们最适合理解这些 plugin 形状：

- action plugin
- hook / resolve plugin
- system text plugin
- runtime HTTP plugin
- 一般 SDK 用户也能直接理解的 lifecycle plugin

### 平台内建或基础设施型 built-ins

这组也是 built-in，但更偏平台内部集成：

- `contact`
- `schedule`
- `task`
- `memory`
- `shell`

这类 plugin 更适合拿来理解平台组装方式，而不是默认假设所有 SDK 用户都会手工挂到本地 `Agent` 上。

## 推荐怎么读

- 如果你关心用户能力型 plugin：先读 `skill`、`web`、`asr`、`tts`、`chat`
- 如果你关心平台规则层：读 `chat` 的内置授权能力
- 如果你关心控制面和状态面：读 `workboard`
- 如果你关心基础设施模式：读 `schedule`、`task`、`memory`、`shell`

## 职责地图

| Plugin | 主要职责 | 主要形状 |
| --- | --- | --- |
| `skill` | 技能发现与安装 | action + system |
| `web` | 联网方法论注入与依赖准备 | action + setup + system |
| `asr` | 语音输入与增强，真实转写函数由 constructor 注入 | action + pipeline + system |
| `tts` | 文本转语音，真实合成函数由 constructor 注入 | action + system |
| `workboard` | 工作快照与状态面 | availability + runtime HTTP |
| `chat` | 渠道 runtime、队列 worker、chat 授权与角色解析 | lifecycle + actions + hooks + resolves + system |
| `contact` | agent 间点对点连接与分享 | action + system + remote protocol |
| `schedule` | plugin action 调度基础设施 | lifecycle-only infrastructure |
| `task` | task 调度与 cron 执行 | lifecycle + actions + system |
| `memory` | memory 存取 | lifecycle + actions + system |
| `shell` | shell session runtime | lifecycle + actions |

## 一个关键区别

不要把“built-in”理解成“它们的公开性完全一样”。

这些内建 plugin 其实分成三类：

- 公开能力型 plugin
- 平台集成型 plugin
- 基础设施型 plugin

也正因为如此，plugin 的概念页保留在 Agent SDK Docs，而具体 plugin 手册单独放在这里。

## 相关概念文档

- [Plugin Overview](/zh/agent-sdk-docs/plugins/overview)
- [Plugin Design Patterns](/zh/agent-sdk-docs/plugins/design-patterns)
- [Plugin Lifecycle](/zh/agent-sdk-docs/plugins/lifecycle)
- [Plugin Availability](/zh/agent-sdk-docs/plugins/plugin-availability)
