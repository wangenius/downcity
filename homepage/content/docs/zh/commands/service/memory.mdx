---
title: city memory
description: Memory service 动作（status/index/search/get/store/flush）
---

# `city memory`

Memory service 命令组。

> 层级说明：`city memory` 是 service 动作命令，不是一级命令。一级入口是 `city service`。

## 用法

```bash
city memory <subcommand> [options]
```

## 子命令

- `status`
- `index`
- `search <query>`
- `get <memoryPath> [--from <line>] [--lines <count>]`
- `store --content <text> [--target <longterm|daily|working>] [--session-id <sessionId>]`
- `flush --session-id <sessionId> [--max-messages <count>]`

## 示例

```bash
city memory status
city memory index --force
city memory search "release decision"
city memory get .downcity/memory/MEMORY.md --from 10 --lines 20
city memory store --target longterm --content "用户偏好简洁的发布说明。"
city memory flush --session-id telegram-chat-123 --max-messages 40
city memory flush --session-id telegram-chat-123 --time "2026-03-25T23:00:00+08:00"
```

## 说明

- Memory 默认开启（零配置）。如需关闭：`context.memory.enabled=false`。
- 默认本地 session 执行链会自动做 recall / capture；`city memory` 主要用于手动检索、写入、重建与排查。
- 当前自动 capture 规则：
- 每轮成功执行后都会写 `working` 与 `daily`
- 当用户输入里出现明显的长期偏好 / 规则信号（例如“记住”“以后”“默认”）时，会额外写 `longterm`
- `longterm` 写入前会先检查 `MEMORY.md`，相同内容不会重复追加
- 当前 `longterm` 会做一层确定性整理，归一为 `preference / fact / decision / rule` 之一
- 所有 memory action 也支持通用 `--delay` / `--time` one-shot 调度。
- 更详细说明见 [`Service Action 定时执行`](/zh/docs/commands/service/scheduling)。
- 先启动 agent：

```bash
city agent start
```

## 示例输出（基于当前实现）

以下示例直接对齐 `packages/downcity/src` 当前命令实现（含默认输出模式与字段结构），示例值使用占位符表示。

```text
$ city memory search "release decision"
{
  "success": true,
  "data": {
    "backend": "builtin",
    "mode": "fts",
    "results": []
  }
}
```

```text
$ city memory flush --session-id <sessionId> --max-messages 40
{
  "success": true,
  "data": {
    "path": ".downcity/memory/daily/2026-04-07.md",
    "messageCount": 12,
    "writtenChars": 864
  }
}
```
