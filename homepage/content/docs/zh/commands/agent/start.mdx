---
title: city agent start
description: 启动 agent runtime（后台或前台）
---

# `city agent start`

启动指定 agent 的 runtime。

## 用法

```bash
city agent start [path] [options]
```

## 常用参数

- `-h, --host <host>`：服务主机（默认 `0.0.0.0`）
- `--foreground [enabled]`：前台运行（不进入后台）

## 行为说明

- 启动前会检查 console 是否已启动。
- `path` 默认当前目录 `.`。
- agent 端口始终由 console 自动分配。
- 默认不依赖项目 `ship.json.start.host/port`。
- `--foreground` 时在当前终端运行；否则按后台方式运行。
- `--foreground` 仅用于当前终端的临时运行会话，终端结束后进程随之结束。

## 相关命令

- [`city agent restart`](/zh/docs/commands/agent/restart)
- [`city agent status`](/zh/docs/commands/agent/status)
- [`city console start`](/zh/docs/commands/console/start)

## 示例输出（基于当前实现）

以下示例直接对齐 `package/src` 当前命令实现（含默认输出模式与字段结构），示例值使用占位符表示。

```text
$ city agent start .
city version: 1.0.0
✅ Downcity daemon started
   pid: <pid>
   log: <daemonLogPath>
```

```text
$ city agent start .
city version: 1.0.0
❌ console is not running. Please run `city console start` first.
```
