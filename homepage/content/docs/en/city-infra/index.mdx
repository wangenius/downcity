---
title: Downcity Infra Docs
description: One InfraRuntime, so your next AI product does not rebuild the backend again.
---

Downcity is a shared AI infrastructure runtime for AI products. It gives you one long-running InfraRuntime that multiple web apps, extensions, apps, desktop tools, client demos, and internal tools can reuse.

What InfraRuntime exposes to products is not a raw provider API. It exposes a stable **product-facing calling boundary**:

- the product holds `product_id + user_token`
- it calls `text()`, `stream()`, `image()`, and other services through `UserClient`
- InfraRuntime keeps the model directory, provider routing, hooks, usage, and env behind the boundary

## Pick your reading path

<div className="dc-doc-grid">
  <a className="dc-doc-card" href="/en/docs/city-infra/quickstart">
    <em>Start Here</em>
    <strong>I want to run one full call chain</strong>
    <p>Deploy an InfraRuntime, add one model, issue a token, and make a product client call `text()` or `stream()` successfully.</p>
  </a>
  <a className="dc-doc-card" href="/en/docs/city-infra/concepts">
    <em>Mental Model</em>
    <strong>I want to understand the boundaries first</strong>
    <p>Separate InfraRuntime, product clients, your business backend, AI gateways, and real providers before implementation details start leaking together.</p>
  </a>
  <a className="dc-doc-card" href="/en/docs/city-infra/guides">
    <em>Real Scenarios</em>
    <strong>I am connecting a real product now</strong>
    <p>Read the practical guides for pure-client products, multi-product reuse, auth, provider env, hooks, and business logic.</p>
  </a>
  <a className="dc-doc-card" href="/en/docs/city-infra/packages">
    <em>Packages</em>
    <strong>I want to read by package</strong>
    <p>Jump straight into `@downcity/infra`, `client`, and official services package by package.</p>
  </a>
  <a className="dc-doc-card" href="/en/docs/city-infra/reference">
    <em>API</em>
    <strong>I only need the interfaces</strong>
    <p>Jump straight to InfraRuntime, UserClient, AdminClient, HTTP API, and environment variables.</p>
  </a>
</div>

## One diagram for the full model

<div className="dc-doc-flow" aria-label="Downcity request flow">
  <div className="dc-doc-lane">
    <div className="dc-doc-node">
      <strong>Many product clients</strong>
      <span>Web apps, extensions, apps, desktop tools, client demos, and internal tools.</span>
      <code>UserClient + product_id + user_token</code>
    </div>
  </div>
  <div className="dc-doc-arrow" aria-hidden="true">→</div>
  <div className="dc-doc-lane">
    <div className="dc-doc-node">
      <strong>Downcity InfraRuntime</strong>
      <span>Verifies the token, resolves the model directory, and dispatches the request to AIService, services, or custom Service actions.</span>
    </div>
    <div className="dc-doc-node">
      <strong>Hooks and usage</strong>
      <span>Quota, balance, billing, logging, and risk control can plug into InfraRuntime while your own business rules stay in your system.</span>
    </div>
  </div>
  <div className="dc-doc-arrow" aria-hidden="true">→</div>
  <div className="dc-doc-lane">
    <div className="dc-doc-node">
      <strong>Upstream execution</strong>
      <span>OpenAI, Claude, Gemini, private models, or your own AI Gateway / proxy.</span>
    </div>
  </div>
</div>

<p className="dc-doc-caption">
  Product clients see stable services and model IDs. Which provider or gateway InfraRuntime uses internally remains a server-side detail.
</p>

## What InfraRuntime owns

- runtime database and Service-owned tables
- provider keys and runtime env
- `user_token` verification and `admin_secret_key` management
- model metadata and handlers through `Provider.model()` + `AIService.use()`
- public model catalogs through `client.ai.listModels()`
- usage records and hook extension points

## What InfraRuntime does not own

- your user system
- orders, subscriptions, CRM, or content data
- product-specific pages, interaction, and business UX
- a public “unified provider API” for arbitrary third-party systems

More precisely:

> InfraRuntime owns the shared AI infrastructure layer.  
> Products still own their users, business logic, interface, and pricing model.

## Recommended order

1. First integration: start at [Quickstart](/en/docs/city-infra/quickstart)
2. Want the full product model: read [Concepts](/en/docs/city-infra/concepts)
3. Connecting a real business scenario: read [Guides](/en/docs/city-infra/guides)
4. Want a package-by-package map: read [Packages](/en/docs/city-infra/packages)
5. Need concrete interfaces: come back to [Reference](/en/docs/city-infra/reference)
