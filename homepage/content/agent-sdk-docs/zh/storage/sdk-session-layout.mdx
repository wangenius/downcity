---
title: SDK Session 布局
description: 本地 SDK Agent 的 session 为什么会落在 .downcity/agents/<agentId>/... 下
---

# SDK Session 布局

SDK 本地 Agent 的 session 默认落盘到：

```text
<projectRoot>/.downcity/agents/<agentId>/sessions/<sessionId>/messages/messages.jsonl
```

## 为什么有 `agentId`

因为同一个项目目录中，可能同时存在多个 SDK Agent。

`agentId` 用来隔离它们的 session 数据。

## 你能在这里看到什么

- `messages.jsonl`
- session metadata
- archive 目录

当你在做多 session 或多 agent 调试时，这套目录结构会非常关键，因为它决定了不同执行单元之间的数据隔离方式。

## 相关文档

- [Session 元数据与落盘](/zh/agent-sdk-docs/sessions/metadata-and-storage)
