---
title: city service stop
description: 面向指定 agent 目标的高级 service 停止命令
---

# `city service stop`

为指定 agent 目标停止某个 service。

日常使用更推荐直接走具体命令根，例如 `city memory stop` 或 `city task stop`。

## 用法

```bash
city service stop <serviceName> [--agent <name> | --path <path>] [--host <host>] [--port <port>]
```

## 示例

```bash
city service stop task --agent my-agent
city memory stop --path /abs/path/to/agent
```

## 相关命令

- [`city service start`](/zh/docs/commands/service/start)
- [`city service status`](/zh/docs/commands/service/status)

## 示例输出（基于当前实现）

以下示例直接对齐 `packages/downcity/src` 当前命令实现（含默认输出模式与字段结构），示例值使用占位符表示。

```text
$ city service stop task
{
  "success": true,
  "service": "task"
}
```

```text
$ city service stop task --agent missing-agent
{
  "success": false,
  "error": "Agent not found in console registry: missing-agent. Run \"city agent list\" to inspect names."
}
```
